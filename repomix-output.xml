This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
docs/
  ARCHITECTURE.md
  PRODUCT.md
  ROADMAP.md
  WORLD_RULES.md
public/
  file.svg
  globe.svg
  next.svg
  vercel.svg
  window.svg
src/
  app/
    favicon.ico
    globals.css
    layout.tsx
    page.tsx
  components/
    district-board.tsx
  data/
    board.test.ts
    board.ts
    developers.ts
    fixtures.ts
  lib/
    github/
      normalize.test.ts
      normalize.ts
      types.ts
    world/
      derive.test.ts
      derive.ts
      rules.ts
      types.ts
    developers.test.ts
    developers.ts
.env.example
.gitignore
AGENTS.md
CLAUDE.md
eslint.config.mjs
next.config.ts
package.json
postcss.config.mjs
README.md
tsconfig.json
vitest.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/lib/developers.test.ts">
import { describe, expect, it, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { loadDeveloper } from "./developers";

describe("loadDeveloper live-source failures", () => {
  it("does not present a recorded profile for a definitive 404", async () => {
    vi.stubEnv("BUILDBLOCK_DATA_MODE", "live");
    vi.stubGlobal("fetch", vi.fn(async () => new Response(null, { status: 404 })));
    await expect(loadDeveloper("jayjz")).resolves.toBeNull();
    vi.unstubAllGlobals();
    vi.unstubAllEnvs();
  });

  it("uses the explicit recorded source for transient upstream failure", async () => {
    vi.stubEnv("BUILDBLOCK_DATA_MODE", "live");
    vi.stubGlobal("fetch", vi.fn(async () => new Response(null, { status: 503 })));
    await expect(loadDeveloper("jayjz")).resolves.toMatchObject({ source: "recorded" });
    vi.unstubAllGlobals();
    vi.unstubAllEnvs();
  });
});
</file>

<file path=".env.example">
GITHUB_TOKEN=
BUILDBLOCK_DATA_MODE=live
</file>

<file path="public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="src/components/district-board.tsx">
"use client";

import { useState } from "react";
import Image from "next/image";
import type { DeveloperExperience } from "@/lib/developers";
import { boardSpaces } from "@/data/board";

const labels = { code_pushed: "Code pushed", pull_request_opened: "Pull request opened", pull_request_merged: "Pull request merged", release_published: "Release published" };
function Building({ experience }: { experience: DeveloperExperience }) {
  const { developer, profile, state } = experience;
  return <div className={`building building--${developer.shape} building--${developer.color}`}>
    {state.releaseEvents.length > 0 && <span className="beacon" aria-label="Release beacon" />}{state.mergeEvents.length > 0 && <span className="pennant" aria-label="Merge pennant">◆</span>}
    <div className="roof" /><div className="facade"><div className="windows" aria-label={`${state.windows.filter(Boolean).length} observed workdays in the last seven days`}>{state.windows.map((lit, index) => <span className={lit ? "window window--lit" : "window"} key={index} />)}</div></div><Image src={profile.avatar_url} alt="" className="avatar" width={17} height={17} />
  </div>;
}
export function DistrictBoard({ developers }: { developers: DeveloperExperience[] }) {
  const [selected, setSelected] = useState<DeveloperExperience | null>(developers[0] ?? null); const byLogin = new Map(developers.map((developer) => [developer.developer.login, developer]));
  return <><section className="board-shell" aria-label="BuildBlock district"><div className="district-board">{boardSpaces.map((space) => { const experience = space.login ? byLogin.get(space.login) : undefined; if (experience) return <button key={space.index} className="space space--developer" style={{ gridArea: position(space.index) }} onClick={() => setSelected(experience)} aria-label={`Inspect ${space.label}'s block`}><Building experience={experience} /><span>{space.label}</span></button>; if (space.kind === "civic") return <div key={space.index} className="space space--civic" style={{ gridArea: position(space.index) }}><span>{space.label}</span></div>; return <div key={space.index} className="space space--open" style={{ gridArea: position(space.index) }} aria-label="Open plot"><i /><span>Open plot</span></div>; })}<div className="commons"><p className="eyebrow">BUILD BLOCK / DISTRICT 01</p><h1>A place built from real work.</h1><p>Public GitHub activity gives each block a temporary pulse. The address stays put.</p><button onClick={() => setSelected(developers[0])}>Explore jayjz’s block</button></div></div></section>{selected && <aside className="detail" aria-live="polite"><button className="detail-close" onClick={() => setSelected(null)} aria-label="Close block details">×</button><div className="detail-heading"><Image src={selected.profile.avatar_url} alt="" width={58} height={58} /><div><p className="eyebrow">BLOCK {selected.developer.address.toString().padStart(2, "0")}</p><h2>{selected.profile.name ?? selected.profile.login}</h2><a href={selected.profile.html_url} target="_blank" rel="noreferrer">@{selected.profile.login} ↗</a></div></div><p className="source-pill">{selected.source === "live" ? "Live public GitHub data" : "Recorded GitHub fixture"} · {new Date(selected.capturedAt).toLocaleDateString()}</p><section><h3>Why this block is lit</h3><p>Each illuminated window represents a UTC day with observed public work in this seven-day view. This is not a score or a claim of complete activity.</p>{selected.state.explanations.map((text) => <p className="explanation" key={text}>{text}</p>)}</section><section><h3>Observed repositories</h3><ul>{[...new Map(selected.activities.map((activity) => [activity.repository.id, activity])).values()].slice(0, 3).map((activity) => <li key={activity.repository.id}><a href={`https://github.com/${activity.repository.name}`} target="_blank" rel="noreferrer">{activity.repository.name} ↗</a></li>)}</ul></section><section><h3>Recent evidence</h3><ol>{selected.activities.slice(0, 5).map((activity) => <li key={activity.id}><a href={activity.evidenceUrl} target="_blank" rel="noreferrer">{labels[activity.kind]}</a><span>{activity.repository.name}</span></li>)}</ol></section></aside>}</>;
}
function position(index: number) { if (index <= 10) return `1 / ${index + 1}`; if (index <= 20) return `${index - 9} / 11`; if (index <= 30) return `11 / ${31 - index}`; return `${41 - index} / 1`; }
</file>

<file path="src/data/board.test.ts">
import { describe, expect, it } from "vitest";
import { boardSpaces } from "./board";
describe("board topology", () => { it("has 40 stable spaces and configured developer addresses", () => { expect(boardSpaces).toHaveLength(40); expect(new Set(boardSpaces.map((space) => space.index)).size).toBe(40); expect(boardSpaces.filter((space) => space.kind === "developer").map((space) => space.index)).toEqual([1, 5, 11]); }); });
</file>

<file path="src/data/board.ts">
import { curatedDevelopers } from "./developers";
export type BoardSpace = { index: number; kind: "developer" | "open" | "civic"; label: string; login?: string };
const civic = new Map([[0, "District Gate"], [10, "Workshop"], [20, "Commons"], [30, "Observatory"]]);
export const boardSpaces: BoardSpace[] = Array.from({ length: 40 }, (_, index) => {
  const developer = curatedDevelopers.find((entry) => entry.address === index);
  if (developer) return { index, kind: "developer", label: developer.login, login: developer.login };
  if (civic.has(index)) return { index, kind: "civic", label: civic.get(index)! };
  return { index, kind: "open", label: "Open plot" };
});
</file>

<file path="src/data/fixtures.ts">
import type { GitHubEventFact, GitHubProfileFact } from "@/lib/github/types";

type RecordedDeveloper = { capturedAt: string; profile: GitHubProfileFact; events: GitHubEventFact[] };
const push = (id: string, developer: number, login: string, repoId: number, repo: string, occurredAt: string, head: string): GitHubEventFact => ({ id, type: "PushEvent", actor: { id: developer, login }, repo: { id: repoId, name: repo }, created_at: occurredAt, public: true, payload: { head, ref: "refs/heads/main" } });
const pr = (id: string, developer: number, login: string, repoId: number, repo: string, occurredAt: string, action: string, number: number): GitHubEventFact => ({ id, type: "PullRequestEvent", actor: { id: developer, login }, repo: { id: repoId, name: repo }, created_at: occurredAt, public: true, payload: { action, number, pull_request: {} } });

// Captured from public GitHub event endpoints on 2026-09-17. This is not synthetic activity.
export const recordedDevelopers: Record<string, RecordedDeveloper> = {
  jayjz: { capturedAt: "2026-09-17T21:52:07.000Z", profile: { id: 161224442, login: "jayjz", name: "Jayjz", avatar_url: "https://avatars.githubusercontent.com/u/161224442?v=4", html_url: "https://github.com/jayjz" }, events: [
    push("21413175036", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T16:59:15.000Z", "0747730d18cdb692142d9c2b50f9e8bc1ff0c45f"),
    push("21359142884", 161224442, "jayjz", 1152522353, "jayjz/jayjz", "2026-09-17T00:42:46.000Z", "3cf0d26720a3a9715a693404378a3403379f2881"),
    pr("15188066119", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T02:02:16.000Z", "merged", 2),
    pr("15188054065", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T02:01:57.000Z", "opened", 2),
  ] },
  antfu: { capturedAt: "2026-09-16T06:00:00.000Z", profile: { id: 11247099, login: "antfu", name: "Anthony Fu", avatar_url: "https://avatars.githubusercontent.com/u/11247099?v=4", html_url: "https://github.com/antfu" }, events: [
    push("21435990201", 11247099, "antfu", 332422464, "antfu/antfu.me", "2026-09-16T05:02:19.000Z", "e3ff0442327a24b1f389982a19ec8aca704d9245"),
    push("21432446142", 11247099, "antfu", 310226997, "antfu-collective/ni", "2026-09-16T03:24:12.000Z", "2d8c88e9f0d302da75eda36e1a158141714a351e"),
  ] },
  shuding: { capturedAt: "2026-09-16T08:00:00.000Z", profile: { id: 3676859, login: "shuding", name: "Shu Ding", avatar_url: "https://avatars.githubusercontent.com/u/3676859?v=4", html_url: "https://github.com/shuding" }, events: [
    push("21405999173", 3676859, "shuding", 1362860356, "vercel-labs/gpu-lexer", "2026-09-16T03:43:59.000Z", "31b4b5da534947ffb0fb95b9e9337e6ff986b0fc"),
    push("21286439582", 3676859, "shuding", 1362860356, "vercel-labs/gpu-lexer", "2026-09-16T07:32:49.000Z", "18299a78b244e3c6508bbc510ffea9f8b5585d18"),
  ] },
};
</file>

<file path="src/lib/github/normalize.test.ts">
import { describe, expect, it } from "vitest";
import { normalizeActivities } from "./normalize";
import type { GitHubEventFact } from "./types";

const event = (overrides: Partial<GitHubEventFact> = {}): GitHubEventFact => ({ id: "1", type: "PushEvent", actor: { id: 7, login: "dev" }, repo: { id: 9, name: "dev/repo" }, created_at: "2026-09-17T12:00:00.000Z", public: true, payload: { head: "abc" }, ...overrides });
describe("normalizeActivities", () => { it("deduplicates pushes and attributes actor IDs", () => { const push = event(); expect(normalizeActivities([push, { ...push, id: "2" }, event({ actor: { id: 8, login: "other" } })], 7)).toHaveLength(1); }); it("accepts merged but not merely closed pull requests", () => { const closed = event({ type: "PullRequestEvent", payload: { action: "closed", number: 4, pull_request: { merged: false } } }); const merged = event({ id: "3", type: "PullRequestEvent", payload: { action: "merged", number: 4, pull_request: {} } }); expect(normalizeActivities([closed, merged], 7).map((item) => item.kind)).toEqual(["pull_request_merged"]); }); it("excludes unsupported events", () => expect(normalizeActivities([event({ type: "WatchEvent", payload: {} })], 7)).toEqual([])); });

describe("important normalized activity facts", () => {
  it("maps releases and merges to durable evidence URLs", () => {
    const release = event({ id: "release-event", type: "ReleaseEvent", payload: { action: "published", release: { id: 12, html_url: "https://github.com/dev/repo/releases/tag/v1" } } });
    const merge = event({ id: "merge-event", type: "PullRequestEvent", payload: { action: "merged", number: 4, pull_request: { html_url: "https://github.com/dev/repo/pull/4" } } });
    expect(normalizeActivities([release, merge], 7)).toEqual(expect.arrayContaining([
      expect.objectContaining({ kind: "release_published", id: "release:9:12", evidenceUrl: "https://github.com/dev/repo/releases/tag/v1" }),
      expect.objectContaining({ kind: "pull_request_merged", id: "pr-merged:9:4", evidenceUrl: "https://github.com/dev/repo/pull/4" }),
    ]));
  });

  it("selects duplicate representations independently of input order", () => {
    const earlier = event({ id: "z-event", created_at: "2026-09-17T12:00:00.000Z" });
    const later = event({ id: "a-event", created_at: "2026-09-17T13:00:00.000Z" });
    expect(normalizeActivities([earlier, later], 7)).toEqual(normalizeActivities([later, earlier], 7));
    expect(normalizeActivities([earlier, later], 7)[0]?.occurredAt).toBe("2026-09-17T12:00:00.000Z");
  });

  it("excludes private and wrong-actor events", () => {
    expect(normalizeActivities([event({ public: false }), event({ actor: { id: 8, login: "other" } })], 7)).toEqual([]);
  });
});
</file>

<file path="src/lib/world/derive.test.ts">
import { describe, expect, it } from "vitest";
import { deriveBlockState } from "./derive";
import type { NormalizedActivity } from "@/lib/github/types";
const activity = (occurredAt: string, id = occurredAt): NormalizedActivity => ({ id, kind: "code_pushed", developerId: 1, login: "dev", repository: { id: 2, name: "dev/repo" }, occurredAt, evidenceUrl: "https://github.com/dev/repo", sourceEventIds: [id] });
describe("deriveBlockState", () => { it("uses exact UTC observation boundaries and stable identity-free state", () => { const state = deriveBlockState([activity("2026-09-11T00:00:00.000Z"), activity("2026-09-17T23:00:00.000Z"), activity("2026-09-10T23:59:59.000Z")], "2026-09-17T23:30:00.000Z"); expect(state.windows).toEqual([true, false, false, false, false, false, true]); }); });

describe("deriveBlockState signals", () => {
  it("derives merge pennants and release beacons from normalized activity", () => {
    const merged = { ...activity("2026-09-17T10:00:00.000Z", "merge"), kind: "pull_request_merged" as const };
    const released = { ...activity("2026-09-16T10:00:00.000Z", "release"), kind: "release_published" as const };
    const state = deriveBlockState([merged, released], "2026-09-17T23:30:00.000Z");
    expect(state.mergeEvents).toHaveLength(1);
    expect(state.releaseEvents).toHaveLength(1);
  });

  it("excludes future activity from the observation window", () => {
    const state = deriveBlockState([activity("2026-09-18T00:00:00.000Z")], "2026-09-17T23:30:00.000Z");
    expect(state.windows).toEqual([false, false, false, false, false, false, false]);
  });
});
</file>

<file path="src/lib/developers.ts">
import "server-only";
import { findDeveloper, type CuratedDeveloper } from "@/data/developers";
import { recordedDevelopers } from "@/data/fixtures";
import { normalizeActivities } from "@/lib/github/normalize";
import { githubEventSchema, githubProfileSchema, type GitHubEventFact, type GitHubProfileFact, type NormalizedActivity } from "@/lib/github/types";
import { deriveBlockState } from "@/lib/world/derive";

export type DeveloperExperience = { developer: CuratedDeveloper; profile: GitHubProfileFact; activities: NormalizedActivity[]; state: ReturnType<typeof deriveBlockState>; source: "live" | "recorded"; capturedAt: string };
const githubHeaders = { Accept: "application/vnd.github+json", "X-GitHub-Api-Version": "2026-03-10", ...(process.env.GITHUB_TOKEN ? { Authorization: `Bearer ${process.env.GITHUB_TOKEN}` } : {}) };
class GitHubProfileNotFoundError extends Error {}

async function getLive(login: string) {
  const [profileResponse, eventResponse] = await Promise.all([
    fetch(`https://api.github.com/users/${login}`, { headers: githubHeaders, next: { revalidate: 3600 } }),
    fetch(`https://api.github.com/users/${login}/events/public?per_page=100`, { headers: githubHeaders, next: { revalidate: 3600 } }),
  ]);
  if (profileResponse.status === 404) throw new GitHubProfileNotFoundError("GitHub profile was not found");
  if (!profileResponse.ok || !eventResponse.ok) throw new Error("GitHub public data is unavailable");
  return { profile: githubProfileSchema.parse(await profileResponse.json()), events: githubEventSchema.array().parse(await eventResponse.json()) };
}

export async function loadDeveloper(login: string): Promise<DeveloperExperience | null> {
  const developer = findDeveloper(login); if (!developer) return null;
  const recorded = recordedDevelopers[login];
  const mode = process.env.BUILDBLOCK_DATA_MODE;
  let source: "live" | "recorded" = "recorded";
  let profile = recorded.profile; let events: GitHubEventFact[] = recorded.events; let capturedAt = recorded.capturedAt;
  if (mode !== "recorded") {
    try {
      const live = await getLive(login); profile = live.profile; events = live.events; capturedAt = new Date().toISOString(); source = "live";
    } catch (error) {
      if (error instanceof GitHubProfileNotFoundError) return null;
      /* transient live-source failures leave the explicit recorded source visible */
    }
  }
  const activities = normalizeActivities(events, developer.id);
  return { developer, profile, activities, state: deriveBlockState(activities, capturedAt), source, capturedAt };
}
</file>

<file path="AGENTS.md">
# BuildBlock

BuildBlock is a persistent multiplayer developer world where real software activity changes the world.

## Product invariants

- Real software work drives world state.
- Every developer should have a recognizable place in the world.
- GitHub source facts must remain separate from game interpretation.
- World state should be deterministic and explainable.
- Collaboration should eventually matter more than raw activity volume.
- The initial product must work without AI.
- AI agents are a future inhabitant of the world, not the foundation of the product.

## Non-goals

Do not turn BuildBlock into:

- a GitHub analytics dashboard with game decoration
- a generic social network
- a commit-count leaderboard
- a Monopoly clone
- a crypto/token project
- an AI agent marketplace
- a premature microservice system

## Architecture boundary

Preserve this dependency direction:

GitHub source data
→ normalized domain facts
→ deterministic world rules
→ derived world state
→ presentation

React components must not interpret raw GitHub API payloads directly.

Keep network IO outside core world logic.

Prefer pure functions for world-state derivation.

## Engineering rules

- TypeScript strictness.
- No fabricated GitHub activity.
- Fixture data must be identifiable as fixtures.
- Live GitHub failure must degrade gracefully.
- No opaque scoring.
- No premature persistence or authentication for P0.
- No unused speculative abstractions.
- Core world-rule calculations require unit tests.
- Accessibility is required.
- Never commit credentials.
- Verify before claiming completion.

## Current phase

P0: prove that a living GitHub-driven developer board is visually and conceptually compelling.

Do not allow later multiplayer, persistence, authentication, or agent phases to contaminate P0.
</file>

<file path="CLAUDE.md">
@AGENTS.md
</file>

<file path="eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
</file>

<file path="postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path="vitest.config.ts">
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    environment: "node",
  },
});
</file>

<file path="docs/ARCHITECTURE.md">
# Architecture

The dependency direction is fixed: GitHub source responses → validated facts → normalized activity → deterministic world events and block state → presentation.

`src/lib/github` validates and normalizes public REST responses. `src/lib/world` is pure and has no network or clock access. `src/lib/developers` is server-only composition; it fetches live public data and falls back to explicitly recorded source facts. React receives `DeveloperExperience`, never raw GitHub responses.

P0 uses only three configured public profiles and no persistence, authentication, or backend services. Next fetch revalidation is the only cache behavior.
</file>

<file path="docs/PRODUCT.md">
# BuildBlock P0

BuildBlock is a persistent developer world where real software work changes a recognizable place. P0 tests whether an evidence-backed public GitHub view makes a developer want a block.

District 01 is a crafted miniature district, not a ranking or analytics dashboard. A block has a permanent address and architectural identity. Recent observed work temporarily lights its facade and may add a merge pennant or release beacon. Curated public profiles do not imply participation or endorsement.

The public event feed is incomplete. BuildBlock therefore says “observed public activity,” never “all activity,” “productivity,” or “inactive.”
</file>

<file path="docs/ROADMAP.md">
# Roadmap

P0 is a public, evidence-backed district with stable blocks and temporary recent activity state. It deliberately excludes ownership, history, multiplayer, authentication, rankings, and AI.

The next gate is qualitative: visitors should recognize a block, explain its visible state through evidence, and want their own place. Only after that should BuildBlock evaluate claimed identity and durable history; collaboration needs verified attribution before it becomes a world rule. Agents are a later inhabitant, never a P0 foundation.
</file>

<file path="docs/WORLD_RULES.md">
# World rules: p0-v1

The observation window is the `asOf` UTC day plus the six preceding UTC days. Each supported activity on a day emits one `workday_observed` event; repeated work on that day does not create more facade illumination.

`code_pushed`, `pull_request_opened`, `pull_request_merged`, and `release_published` can light a day. A merge creates a merge pennant and a release creates a release beacon. Activities are deduplicated by their durable action identity and only source events whose actor ID matches the configured developer are accepted.

Stars, forks, branch creation, comments, counts, and follower data have no effect. A closed pull request is a merge only when GitHub explicitly reports `merged` or `closed` with `merged: true`.
</file>

<file path="src/app/globals.css">
@import "tailwindcss";
:root { --ink:#18302b; --paper:#f5f0e5; --grass:#7d986b; --road:#d6c9ae; --line:#33483e; }
* { box-sizing:border-box; } body { margin:0; color:var(--ink); background:#e5dccb; font-family:Georgia, "Times New Roman", serif; } button { font:inherit; } main { min-height:100vh; padding:clamp(12px,3vw,42px); background:radial-gradient(circle at 50% 35%,#f8f4eb 0 22%,transparent 55%),#dfe5d2; }
.board-shell { overflow:auto; border:1px solid #83917c; background:#becaae; box-shadow:0 18px 50px #40523935; padding:12px; }
.district-board { display:grid; grid-template:repeat(11, minmax(78px,1fr))/repeat(11,minmax(78px,1fr)); min-width:858px; aspect-ratio:1; position:relative; background:linear-gradient(90deg,transparent 49.5%,#a7b894 50% 50.5%,transparent 51%),linear-gradient(#edf0df,#d7e1c8); }
.space { border:1px solid #a89675; background:var(--road); min-width:0; display:flex; flex-direction:column; align-items:center; justify-content:center; color:#304237; font-size:10px; letter-spacing:.04em; text-transform:uppercase; gap:4px; }.space--developer { cursor:pointer; padding:3px; transition:filter .15s, transform .15s; }.space--developer:hover,.space--developer:focus-visible { filter:brightness(1.1); transform:translateY(-2px); outline:3px solid #d58832; outline-offset:-3px; z-index:2; }.space--civic { background:#566f61; color:#f7efd9; font-weight:bold; }.space--open { color:#78816b; }.space--open i { width:24px; height:12px; border:2px solid #839570; border-radius:50%; }
.commons { grid-area:2/2/11/11; display:flex; flex-direction:column; align-items:center; justify-content:center; text-align:center; padding:50px; background:radial-gradient(ellipse at center,#f7f1dd 0 32%,#c6d1b3 33% 37%,transparent 38%),linear-gradient(135deg,#dbe4c7,#bdcfad); }.commons h1 { margin:6px 0; max-width:550px; font-size:clamp(32px,5vw,68px); letter-spacing:-.06em; line-height:.92; }.commons p:not(.eyebrow) { max-width:370px; line-height:1.45; }.eyebrow { margin:0; color:#a65b29; font-family:ui-monospace,monospace; font-size:10px; letter-spacing:.12em; }.commons button { margin-top:12px; background:#213d34; color:white; border:0; padding:12px 18px; border-radius:2px; cursor:pointer; }
.building { width:46px; height:46px; position:relative; filter:drop-shadow(3px 4px 0 #4f5a46aa); }.roof { width:38px; height:13px; background:#4d4b42; clip-path:polygon(50% 0,100% 100%,0 100%); margin:auto; }.facade { height:29px; width:36px; margin:auto; background:#bd7a42; border:2px solid #554633; }.building--slate .facade{background:#70828a}.building--terracotta .facade{background:#ad5c43}.building--tower{height:55px}.building--tower .facade{height:37px}.building--row .facade{width:44px}.avatar { width:17px; height:17px; object-fit:cover; border-radius:50%; border:1px solid white; position:absolute; bottom:-4px; right:0; }.windows { display:grid; grid-template-columns:repeat(3,1fr); gap:3px; padding:5px; }.window { width:6px; height:7px; background:#31413b; }.window--lit { background:#ffe49a; box-shadow:0 0 8px #ffd56b; }.pennant { position:absolute; right:-7px; top:4px; color:#f4d56d; font-size:13px; }.beacon { position:absolute; top:-7px; left:20px; width:7px; height:7px; border-radius:50%; background:#f6c75c; box-shadow:0 0 10px 4px #f6c75caa; }
.detail { position:fixed; right:20px; top:20px; width:min(390px,calc(100vw - 40px)); max-height:calc(100vh - 40px); overflow:auto; background:#f9f4e8; border:1px solid #9c8d72; box-shadow:0 16px 55px #263c3388; padding:28px; z-index:5; }.detail-close { position:absolute; right:12px; top:10px; border:0; background:none; font-size:25px; cursor:pointer; }.detail-heading { display:flex; gap:14px; align-items:center; }.detail-heading img { width:58px; height:58px; border-radius:50%; }.detail h2 { margin:4px 0; }.detail a { color:#8d431f; }.detail section { border-top:1px solid #d9cdb5; margin-top:18px; padding-top:12px; }.detail h3 { margin:0 0 8px; }.detail p,.detail li { font-size:14px; line-height:1.45; }.source-pill { display:inline-block; background:#e4d6b8; padding:5px 8px; font-family:ui-monospace,monospace; font-size:10px !important; }.explanation { color:#70502c; }.detail ol { padding-left:18px; }.detail li span { display:block; color:#667069; font-size:12px; }
@media (max-width:700px) { main { padding:0; }.board-shell { border:0; }.detail { right:0; top:auto; bottom:0; width:100%; max-height:62vh; } } @media (prefers-reduced-motion:reduce) { * { transition:none !important; } }
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = { title: "BuildBlock — District 01", description: "A living developer district shaped by public GitHub activity." };
export default function RootLayout({ children }: LayoutProps<"/">) { return <html lang="en"><body>{children}</body></html>; }
</file>

<file path="src/app/page.tsx">
import { DistrictBoard } from "@/components/district-board";
import { loadDeveloper } from "@/lib/developers";

export default async function Home() {
  const loaded = await Promise.all(["jayjz", "antfu", "shuding"].map(loadDeveloper));
  return <main><DistrictBoard developers={loaded.filter((entry): entry is NonNullable<typeof entry> => entry !== null)} /></main>;
}
</file>

<file path="src/data/developers.ts">
export type CuratedDeveloper = { id: number; login: string; address: number; color: string; shape: "tower" | "row" | "workshop" };
export const curatedDevelopers: CuratedDeveloper[] = [
  { id: 161224442, login: "jayjz", address: 1, color: "ochre", shape: "workshop" },
  { id: 11247099, login: "antfu", address: 5, color: "terracotta", shape: "tower" },
  { id: 3676859, login: "shuding", address: 11, color: "slate", shape: "row" },
];
export const findDeveloper = (login: string) => curatedDevelopers.find((developer) => developer.login === login);
</file>

<file path="src/lib/github/normalize.ts">
import type { GitHubEventFact, NormalizedActivity } from "./types";

const repositoryUrl = (name: string) => `https://github.com/${name}`;

const duplicatePreferenceKey = (activity: NormalizedActivity) => [
  activity.occurredAt,
  activity.evidenceUrl,
  activity.login,
  activity.repository.name,
  activity.sourceEventIds[0] ?? "",
].join("\u0000");

function preferDuplicate(current: NormalizedActivity, candidate: NormalizedActivity) {
  return duplicatePreferenceKey(candidate) < duplicatePreferenceKey(current) ? candidate : current;
}

export function normalizeActivities(events: GitHubEventFact[], developerId: number): NormalizedActivity[] {
  const accepted = events
    .filter((event) => event.public && event.actor.id === developerId)
    .flatMap((event): NormalizedActivity[] => {
      const base = { developerId, login: event.actor.login, repository: event.repo, occurredAt: event.created_at, sourceEventIds: [event.id] };
      if (event.type === "PushEvent" && event.payload.head) return [{ ...base, id: `push:${event.repo.id}:${event.payload.head}`, kind: "code_pushed", evidenceUrl: `${repositoryUrl(event.repo.name)}/commit/${event.payload.head}` }];
      if (event.type === "PullRequestEvent" && event.payload.number) {
        const url = event.payload.pull_request?.html_url ?? `${repositoryUrl(event.repo.name)}/pull/${event.payload.number}`;
        if (event.payload.action === "opened") return [{ ...base, id: `pr-opened:${event.repo.id}:${event.payload.number}`, kind: "pull_request_opened", evidenceUrl: url }];
        if (event.payload.action === "merged" || (event.payload.action === "closed" && event.payload.pull_request?.merged === true)) return [{ ...base, id: `pr-merged:${event.repo.id}:${event.payload.number}`, kind: "pull_request_merged", evidenceUrl: url }];
      }
      if (event.type === "ReleaseEvent" && event.payload.action === "published" && event.payload.release?.id) return [{ ...base, id: `release:${event.repo.id}:${event.payload.release.id}`, kind: "release_published", evidenceUrl: event.payload.release.html_url ?? repositoryUrl(event.repo.name) }];
      return [];
    });
  const deduplicated = new Map<string, NormalizedActivity>();
  for (const activity of accepted) {
    const current = deduplicated.get(activity.id);
    deduplicated.set(activity.id, current ? preferDuplicate(current, activity) : activity);
  }
  return [...deduplicated.values()].sort((a, b) => b.occurredAt.localeCompare(a.occurredAt) || a.id.localeCompare(b.id));
}
</file>

<file path="src/lib/github/types.ts">
import { z } from "zod";

const actorSchema = z.object({ id: z.number(), login: z.string() });
const repositorySchema = z.object({ id: z.number(), name: z.string() });

export const githubProfileSchema = z.object({
  id: z.number(), login: z.string(), name: z.string().nullable(), avatar_url: z.string().url(), html_url: z.string().url(),
});

export const githubEventSchema = z.object({
  id: z.string(), type: z.string(), actor: actorSchema, repo: repositorySchema, created_at: z.string().datetime(), public: z.boolean().optional().default(true),
  payload: z.object({
    head: z.string().optional(), ref: z.string().optional(), action: z.string().optional(), number: z.number().optional(),
    pull_request: z.object({ merged: z.boolean().nullable().optional(), html_url: z.string().url().nullable().optional() }).optional(),
    release: z.object({ id: z.number().optional(), html_url: z.string().url().nullable().optional() }).optional(),
  }).passthrough(),
}).passthrough();

export type GitHubProfileFact = z.infer<typeof githubProfileSchema>;
export type GitHubEventFact = z.infer<typeof githubEventSchema>;
export type ActivityKind = "code_pushed" | "pull_request_opened" | "pull_request_merged" | "release_published";

export type NormalizedActivity = {
  id: string; kind: ActivityKind; developerId: number; login: string; repository: { id: number; name: string };
  occurredAt: string; evidenceUrl: string; sourceEventIds: string[];
};
</file>

<file path="src/lib/world/derive.ts">
import type { NormalizedActivity } from "@/lib/github/types";
import { deriveWorldEvents } from "./rules";
import type { BlockState } from "./types";

const dayKey = (value: string) => value.slice(0, 10);
export function deriveBlockState(activities: NormalizedActivity[], asOf: string): BlockState {
  const end = new Date(asOf); end.setUTCHours(0, 0, 0, 0);
  const days = Array.from({ length: 7 }, (_, index) => new Date(end.getTime() - (6 - index) * 86_400_000).toISOString().slice(0, 10));
  const eligible = activities.filter((activity) => activity.occurredAt <= asOf && days.includes(dayKey(activity.occurredAt)));
  const events = deriveWorldEvents(eligible);
  const merges = events.filter((event) => event.kind === "merge_observed");
  const releases = events.filter((event) => event.kind === "release_observed");
  return { ruleVersion: "p0-v1", asOf, windows: days.map((day) => eligible.some((activity) => dayKey(activity.occurredAt) === day)), mergeEvents: merges, releaseEvents: releases,
    explanations: [
      ...merges.map((event) => `Merge pennant: observed merge in ${event.activity.repository.name}.`),
      ...releases.map((event) => `Release beacon: observed release in ${event.activity.repository.name}.`),
    ] };
}
</file>

<file path="src/lib/world/rules.ts">
import type { NormalizedActivity } from "@/lib/github/types";
import type { WorldEvent } from "./types";

export function deriveWorldEvents(activities: NormalizedActivity[]): WorldEvent[] {
  return activities.flatMap((activity) => {
    const events: WorldEvent[] = [{ id: `workday:${activity.id}`, kind: "workday_observed", effectiveAt: activity.occurredAt, activity }];
    if (activity.kind === "pull_request_merged") events.push({ id: `merge:${activity.id}`, kind: "merge_observed", effectiveAt: activity.occurredAt, activity });
    if (activity.kind === "release_published") events.push({ id: `release:${activity.id}`, kind: "release_observed", effectiveAt: activity.occurredAt, activity });
    return events;
  });
}
</file>

<file path="src/lib/world/types.ts">
import type { NormalizedActivity } from "@/lib/github/types";

export type WorldEvent = { id: string; kind: "workday_observed" | "merge_observed" | "release_observed"; effectiveAt: string; activity: NormalizedActivity };
export type BlockState = { ruleVersion: "p0-v1"; asOf: string; windows: boolean[]; mergeEvents: WorldEvent[]; releaseEvents: WorldEvent[]; explanations: string[] };
</file>

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local Python environment
/venv/
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [{ protocol: "https", hostname: "avatars.githubusercontent.com" }],
  },
};

export default nextConfig;
</file>

<file path="package.json">
{
  "name": "buildblock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.5",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
</file>

<file path="README.md">
# BuildBlock

BuildBlock is a persistent developer world where public GitHub activity changes a recognizable block in a shared district.

## Run locally

```bash
npm install
npm run dev
```

For a reproducible offline board, copy `.env.example` to `.env.local` and set `BUILDBLOCK_DATA_MODE=recorded`.

## Verify

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

See `docs/` for product, architecture, world-rule, and roadmap decisions.
</file>

</files>
