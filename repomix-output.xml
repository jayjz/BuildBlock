This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
docs/
  ARCHITECTURE.md
  PLACE_SYSTEM.md
  PRODUCT.md
  ROADMAP.md
  WORLD_RULES.md
public/
  file.svg
  globe.svg
  next.svg
  vercel.svg
  window.svg
src/
  app/
    favicon.ico
    globals.css
    layout.tsx
    page.tsx
  components/
    developer-workshop.tsx
    district-board.tsx
    district-commons.tsx
    workshop-detail.tsx
  data/
    board.test.ts
    board.ts
    developers.ts
    fixtures.ts
  lib/
    github/
      normalize.test.ts
      normalize.ts
      types.ts
    world/
      derive.test.ts
      derive.ts
      rules.ts
      types.ts
    developers.test.ts
    developers.ts
.env.example
.gitignore
AGENTS.md
CLAUDE.md
eslint.config.mjs
next.config.ts
package.json
postcss.config.mjs
README.md
tsconfig.json
vitest.config.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="docs/PLACE_SYSTEM.md">
# BuildBlock Place System

## Status

**Phase:** P0
**Purpose:** Define the canonical spatial model for BuildBlock before implementation expands beyond the first living district.

This document is the durable product decision for how developers, repositories, recent work, and visible world state are represented spatially.

It complements:

* `docs/PRODUCT.md`
* `docs/ARCHITECTURE.md`
* `docs/WORLD_RULES.md`
* `docs/ROADMAP.md`

It does not replace the evidence or world-rule contracts defined elsewhere.

---

# Core decision

A developer block is a **permanent workshop** with a public work frontage displaying repositories observed in recent software activity.

The workshop represents the developer's persistent place in BuildBlock.

Repositories do not permanently own parts of that place during P0.

Recent observable work temporarily changes what is displayed inside the workshop.

---

# Product intent

BuildBlock should feel like a place built from software work, not a GitHub dashboard rendered onto buildings.

A visitor should be able to look at a workshop and understand:

* who occupies the place
* which repositories have recently appeared in observed public activity
* whether merge or release activity has been observed for those repositories
* that the visible state represents a bounded observation window, not the developer's complete work

A visitor should not need to open a detail panel before the workshop has meaning.

---

# Canonical spatial grammar

## Public work apron

The P0 workshop frontage is a large, inward-facing public work apron rather than a
repository dashboard inside the building facade. Each occupied parcel has the same
spatial budget and visibly connects its canonical perimeter address through a
threshold, workshop building, apron workbench, and shared pedestrian path.

The workshop building carries fixed identity: its configured silhouette, material,
developer nameplate, and address threshold. The apron carries temporary observed
work: up to three full repository placards in canonical repository order, plus a
source and capture caption. Repository placards are mounted on one shared physical
bench and may wrap; presentation must not reduce their text below normal reading
size to preserve a fixed parcel height.

An observed merge is represented once per repository as two joined pieces. An
observed release is represented once per repository as a plain publication bundle
with a dispatch label. These are evidence markers only: they do not imply
completion, deployment, quality, or delivery success. A small, always-visible key
states their source meanings in text.

On desktop, the compact rectangular district keeps the 40-address perimeter and
four civic corners while the three configured parcel presentations share the
interior. The center is a small orientation landing and pedestrian path, not the
district focal point. Below the desktop breakpoint, the perimeter becomes a compact
orientation map and the same parcel components appear in address order as a
readable vertical sequence. Inspection details open below the district so that
selecting a workshop never shrinks the board.

## Developer parcel

Each configured developer has a fixed parcel and address.

Stable parcel identity includes:

* address
* developer identity
* configured workshop silhouette
* configured material or color character
* threshold or nameplate

These are identity choices, not measures of achievement.

Recent GitHub activity must never change:

* parcel size
* land ownership
* building size
* apparent wealth
* architectural importance

---

## Workshop

The workshop is the developer's primary physical representation.

It remains present regardless of recent observable activity.

The workshop must not visually decay or imply abandonment when no qualifying public activity is observed.

P0 does not infer:

* productivity
* quality
* current online presence
* private activity
* professional skill
* project completion

---

## Repositories

Repositories represent **recently observed work**, not permanent property.

All eligible observed repositories must be derived in the domain layer.

Presentation may display up to three repositories on the workshop frontage.

The three-repository limit is a presentation constraint only.

It must never truncate the derived domain state.

If more than three repositories are eligible:

* display the first three on the workshop frontage
* expose the remaining observed repositories in the detail experience
* use explicit wording such as `+N more observed repositories`

Repository presence must never imply:

* complete project inventory
* repository ownership
* primary-project status
* project quality
* project importance

Preferred label:

> Recently observed here

---

# Repository ordering

Visible repository frontage should be ordered by:

1. most recent eligible observed activity, descending
2. repository ID as deterministic tie-breaker

Do not order repositories by:

* activity volume
* stars
* followers
* commit count
* perceived importance
* repository popularity

Repository ordering exists for temporal orientation, not ranking.

The result must remain deterministic regardless of source input ordering.

---

# Temporary observed state

Temporary state is recomputed from evidence within the existing P0 observation window.

It includes:

* repository frontage
* work-bay state
* merge state
* release state
* source provenance
* observation timestamps

Temporary state does not accumulate.

When supporting evidence leaves the observation window, the corresponding visible state disappears without implying that work was deleted or abandoned.

Recorded fixture state remains anchored to its recorded capture time.

---

# Event-to-world grammar

## `code_pushed`

Meaning:

Observed public code activity exists for the repository.

Spatial consequence:

* repository may occupy a visible work bay
* restrained task-light or workbench-active state may be shown

It does not mean:

* high productivity
* meaningful progress
* project completion
* developer currently working

Repeated pushes do not increase brightness, workshop size, decoration count, or apparent status.

---

## `pull_request_opened`

Meaning:

An observed public pull request was opened.

Spatial consequence:

* repository participates in general observed-work state
* explicit event remains visible in evidence/details

No unique physical queue or review state is required in P0.

It does not mean:

* the PR is still open
* it is awaiting review
* it will be merged

---

## `pull_request_merged`

Meaning:

An observed public pull request merge is supported by the source contract.

Spatial consequence:

* repository receives a restrained merge-specific visual state
* preferred metaphor: a joined-piece or integration inset attached to that repository's work bay

It does not mean:

* project completion
* passing CI
* successful deployment
* quality

Multiple merge events do not multiply the visual state.

---

## `release_published`

Meaning:

An observed public release was published.

Spatial consequence:

* repository receives a restrained publication/shipment marker
* preferred metaphor: a small threshold shipment, crate, or publication marker

Avoid gift-box or celebratory-cartoon styling.

It does not mean:

* deployment success
* adoption
* downloads
* production readiness
* project importance

Multiple releases do not multiply the visual state.

---

# Public silence

When no qualifying public activity exists inside the observation window:

* the workshop remains intact
* the developer address remains visible
* permanent architectural identity remains unchanged
* temporary repository displays may be empty

Acceptable language:

> No public work observed in this window.

Do not use:

* inactive
* abandoned
* idle developer
* low activity
* dormant

GitHub public event coverage is incomplete and cannot support those claims.

---

# Stable identity vs activity

This separation is fundamental.

## Stable identity

* developer
* address
* parcel
* workshop silhouette
* configured visual character
* threshold/nameplate

## Temporary observed state

* repository displays
* task-light state
* merge state
* release state
* evidence
* observation timestamps

Activity must never redefine permanent identity.

---

# Future durable progression

Durable progression is explicitly deferred from P0.

Potential future concepts may include:

* persistent workshop annexes
* historically grounded project spaces
* verified collaboration infrastructure
* release exhibits
* district landmarks

These require:

* claimed identity
* durable historical storage
* explicit progression rules
* validated product need

P0 must not simulate these systems visually before they exist.

---

# District model

## Perimeter

Preserve the existing 40-address district topology.

Developer addresses remain stable regardless of recent activity.

---

## Open plots

Open plots represent unassigned future space.

They should appear as quiet surveyed ground or address markers.

Do not add:

* fake claiming controls
* purchase language
* scarcity messaging
* waitlists
* implied ownership

---

## Civic spaces

The four civic spaces remain fixed landmarks.

They provide orientation and identity, not rewards or game mechanics.

P0 civic spaces should remain primarily static.

---

## District center

The district center should function as a restrained wayfinding court.

Its job is:

* orientation
* District 01 identity
* finding occupied workshops
* supporting movement between workshops

It should not become:

* a dashboard
* analytics view
* leaderboard
* KPI surface
* giant landing-page hero
* dominant navigation menu

The workshops should remain the primary visual subjects.

---

# Exploration loop

The intended first-visit flow is:

1. visitor lands with no developer detail automatically open
2. occupied workshops and addresses are visible immediately
3. recently observed repository names create specific points of curiosity
4. visitor selects a workshop
5. detail view explains the same repository-grouped state shown on the frontage
6. visitor may follow original evidence links
7. visitor can move directly to another configured workshop
8. the next workshop exposes different observed work

The second workshop should feel meaningfully different because the work represented there is different, not because it has a different activity score.

---

# Detail experience

The detail experience must use the same observation window and derived repository groups as the workshop frontage.

Do not independently regroup the unrestricted activity feed inside React.

Details should expose:

* developer identity
* address
* source mode
* capture date
* precise observation window
* observed repositories
* supported activities
* evidence links
* limitations of public GitHub coverage

The detail experience should clarify the world state rather than replace it.

---

# Presentation constraints

Prefer:

* semantic HTML
* CSS
* small inline or componentized SVG
* restrained architectural forms
* tactile materials
* clear typography
* static environmental detail

Avoid:

* dashboard cards as the primary interaction model
* contribution-calendar visuals
* neon AI styling
* glassmorphism
* fake terminal interfaces
* literal Monopoly imitation
* Web3 aesthetics
* arbitrary animations
* game-engine complexity

Activity-dependent movement or animation must have semantic justification.

---

# Accessibility

The spatial metaphor must remain usable without relying on visual interpretation alone.

Requirements include:

* visible keyboard focus
* usable keyboard interaction
* minimum practical 44px interaction targets
* state communicated by label/shape in addition to color
* repository names that wrap safely
* no interaction requiring hover
* reduced-motion support
* appropriate focus movement when opening and closing detail views

Decorative SVG should be hidden from assistive technology where appropriate.

---

# P0 boundaries

Do not add:

* authentication
* claiming
* persistence
* history
* rankings
* scores
* currency
* property mechanics
* chat
* follower systems
* AI summaries
* agents
* autonomous behavior
* project-description inference
* technology inference
* collaboration inference
* additional GitHub endpoints solely to make the workshop richer

P0 exists to test the spatial product hypothesis before expanding the system.

---

# P0 product hypothesis

BuildBlock should test:

> Does representing real software work as a changing physical place make developers want a place of their own?

The repository-fronted workshop slice is intended to improve our ability to answer that question.

---

# Implementation direction

The next vertical slice should:

* preserve the current evidence pipeline
* extend derived block state with all eligible observed repositories
* keep repository grouping in the domain layer
* show up to three repositories on the workshop frontage
* order frontage by most recent eligible observed activity
* attach merge and release state to the relevant repository
* replace the seven-window facade
* keep details closed on initial load
* introduce restrained wayfinding
* preserve fixed developer addresses and workshop identity
* expose source and observation limitations clearly

No durable progression is implemented during this slice.
</file>

<file path="public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="src/components/developer-workshop.tsx">
import type { CuratedDeveloper } from "@/data/developers";
import type { DeveloperExperience } from "@/lib/developers";

type DeveloperWorkshopProps = {
  developer: CuratedDeveloper;
  experience?: DeveloperExperience;
  selected: boolean;
  onInspect?: () => void;
  workshopRef?: (element: HTMLButtonElement | null) => void;
};

const address = (value: number) => value.toString().padStart(2, "0");
const captureLabel = (value: string) => new Intl.DateTimeFormat("en", { month: "short", day: "numeric", year: "numeric", timeZone: "UTC" }).format(new Date(value));

export function DeveloperWorkshop({ developer, experience, selected, onInspect, workshopRef }: DeveloperWorkshopProps) {
  const repositories = experience?.state.repositories.slice(0, 3) ?? [];
  const resident = experience?.profile.name ?? developer.login;
  const moreCount = (experience?.state.repositories.length ?? 0) - repositories.length;
  const frontageSummary = repositories.map((group) => {
    const states = ["recently observed work"];
    if (group.hasMerge) states.push("pull request merge observed");
    if (group.hasRelease) states.push("release published");
    return `${group.repository.name}, ${states.join(", ")}`;
  }).join("; ");
  const label = `Inspect ${resident}, workshop ${address(developer.address)}. ${repositories.length === 0 ? "No public work observed in this window." : `Recently observed here: ${frontageSummary}.`}`;

  if (!experience) {
    return <div className={`workshop workshop--${developer.shape} workshop--${developer.color} workshop--unavailable`} aria-label={`${developer.login}, address ${address(developer.address)}, workshop unavailable`}>
      <WorkshopStructure developer={developer} resident={resident} repositories={[]} moreCount={0} unavailable />
    </div>;
  }

  return <button type="button" ref={workshopRef} className={`workshop workshop--${developer.shape} workshop--${developer.color}`} onClick={onInspect} aria-pressed={selected} aria-label={label}>
    <WorkshopStructure developer={developer} resident={resident} repositories={repositories} moreCount={moreCount} source={experience.source} capturedAt={experience.capturedAt} />
  </button>;
}

type WorkshopStructureProps = {
  developer: CuratedDeveloper;
  resident: string;
  repositories: DeveloperExperience["state"]["repositories"][number][];
  moreCount: number;
  source?: DeveloperExperience["source"];
  capturedAt?: string;
  unavailable?: boolean;
};

function WorkshopStructure({ developer, resident, repositories, moreCount, source, capturedAt, unavailable }: WorkshopStructureProps) {
  return <>
    <span className="workshop-building" aria-hidden="true">
      <span className="workshop-roof"><i /><i /></span>
      <span className="workshop-facade"><span className="workshop-window" /><span className="workshop-door" /><span className="workshop-window" /></span>
      <span className="workshop-nameplate">{resident} <small>@{developer.login}</small></span>
    </span>
    <span className="workshop-threshold"><strong>{address(developer.address)}</strong><em>threshold</em></span>
    <span className="work-apron">
      <span className="apron-heading">Recently observed here</span>
      <span className="workbench">
        {repositories.length === 0 ? <span className="workbench-empty">{unavailable ? "Workshop unavailable" : "No public work observed in this window."}</span> : repositories.map((group) => <span className="repository-placard" key={group.repository.id}>
          <span className="repository-name">{group.repository.name}</span>
          <span className="repository-objects" aria-hidden="true">
            {group.hasMerge && <span className="repository-object repository-object--merge" title="Pull request merge observed"><i /><i /></span>}
            {group.hasRelease && <span className="repository-object repository-object--release" title="Release published"><i /></span>}
          </span>
        </span>)}
      </span>
      {moreCount > 0 && <span className="workshop-more">+{moreCount} more observed repositories</span>}
      {source && capturedAt && <span className="workshop-source">{source === "live" ? "Live public GitHub" : "Recorded GitHub fixture"} · captured {captureLabel(capturedAt)}</span>}
    </span>
  </>;
}
</file>

<file path="src/components/district-commons.tsx">
export function DistrictCommons() {
  return <section className="district-commons" aria-label="District 01 orientation landing">
    <div className="commons-copy">
      <p className="eyebrow">BUILD BLOCK</p>
      <h1>District 01</h1>
      <p>Public work district</p>
    </div>
    <div className="evidence-key" aria-label="Evidence key">
      <span><i className="key-object key-object--merge" aria-hidden="true"><b /><b /></i>Joined pieces: pull request merge observed</span>
      <span><i className="key-object key-object--release" aria-hidden="true"><b /></i>Publication bundle: release published</span>
    </div>
  </section>;
}
</file>

<file path="src/components/workshop-detail.tsx">
"use client";

import { useEffect, useRef } from "react";
import Image from "next/image";
import type { DeveloperExperience } from "@/lib/developers";
import type { ActivityKind } from "@/lib/github/types";

const labels: Record<ActivityKind, string> = {
  code_pushed: "Code pushed",
  pull_request_opened: "Pull request opened",
  pull_request_merged: "Pull request merged",
  release_published: "Release published",
};

const dateTime = (value: string) => new Intl.DateTimeFormat("en", { dateStyle: "medium", timeStyle: "short", timeZone: "UTC" }).format(new Date(value)) + " UTC";

type WorkshopDetailProps = { experience: DeveloperExperience; onClose: () => void; onNext: () => void; nextName?: string; hasNext: boolean };

export function WorkshopDetail({ experience, onClose, onNext, nextName, hasNext }: WorkshopDetailProps) {
  const headingRef = useRef<HTMLHeadingElement>(null);
  useEffect(() => { headingRef.current?.focus(); }, [experience.developer.login]);
  const { developer, profile, state } = experience;

  return <aside className="workshop-detail" aria-label={`${profile.name ?? profile.login} workshop details`}>
    <div className="detail-topline"><p className="eyebrow">WORKSHOP {developer.address.toString().padStart(2, "0")}</p><button type="button" className="detail-close" onClick={onClose}>Close</button></div>
    <div className="detail-heading">
      <Image src={profile.avatar_url} alt="" width={58} height={58} />
      <div><h2 ref={headingRef} tabIndex={-1}>{profile.name ?? profile.login}</h2><a href={profile.html_url} target="_blank" rel="noreferrer">@{profile.login} ↗</a></div>
    </div>
    <dl className="detail-facts">
      <div><dt>Source</dt><dd>{experience.source === "live" ? "Live public GitHub data" : "Recorded GitHub fixture"}</dd></div>
      <div><dt>Captured</dt><dd>{dateTime(experience.capturedAt)}</dd></div>
      <div><dt>Observed window</dt><dd>{dateTime(state.observationWindow.startsAt)} to {dateTime(state.observationWindow.endsAt)}</dd></div>
    </dl>
    <section>
      <h3>Recently observed here</h3>
      {state.repositories.length === 0 ? <p>No public work observed in this window.</p> : <ul className="repository-list">
        {state.repositories.map((group) => <li key={group.repository.id} className="repository-group">
          <div className="repository-heading"><a href={`https://github.com/${group.repository.name}`} target="_blank" rel="noreferrer">{group.repository.name} ↗</a><span>Observed {dateTime(group.latestObservedAt)}</span></div>
          <p className="repository-state"><span className="observed-work">Observed work</span>{group.hasMerge && <span className="merge-state">Joined work observed</span>}{group.hasRelease && <span className="release-state">Release observed</span>}</p>
          <ul className="evidence-list">
            {group.activities.map((activity) => <li key={activity.id}><a href={activity.evidenceUrl} target="_blank" rel="noreferrer">{labels[activity.kind]} ↗</a><time dateTime={activity.occurredAt}>{dateTime(activity.occurredAt)}</time></li>)}
          </ul>
        </li>)}
      </ul>}
    </section>
    <section className="detail-limits">
      <h3>What this view covers</h3>
      <p>These are supported public GitHub events observed in this bounded window. They can be incomplete and do not show a complete project inventory, current presence, ownership, productivity, or project quality.</p>
    </section>
    {hasNext && <button type="button" className="next-workshop" onClick={onNext}>Visit {nextName}&apos;s workshop <span aria-hidden="true">→</span></button>}
  </aside>;
}
</file>

<file path="src/data/board.test.ts">
import { describe, expect, it } from "vitest";
import { boardSpaces } from "./board";
describe("board topology", () => { it("has 40 stable spaces and configured developer addresses", () => { expect(boardSpaces).toHaveLength(40); expect(new Set(boardSpaces.map((space) => space.index)).size).toBe(40); expect(boardSpaces.filter((space) => space.kind === "developer").map((space) => space.index)).toEqual([1, 5, 11]); }); });
</file>

<file path="src/data/board.ts">
import { curatedDevelopers } from "./developers";
export type BoardSpace = { index: number; kind: "developer" | "open" | "civic"; label: string; login?: string };
const civic = new Map([[0, "District Gate"], [10, "Workshop"], [20, "Commons"], [30, "Observatory"]]);
export const boardSpaces: BoardSpace[] = Array.from({ length: 40 }, (_, index) => {
  const developer = curatedDevelopers.find((entry) => entry.address === index);
  if (developer) return { index, kind: "developer", label: developer.login, login: developer.login };
  if (civic.has(index)) return { index, kind: "civic", label: civic.get(index)! };
  return { index, kind: "open", label: "Open plot" };
});
</file>

<file path="src/data/fixtures.ts">
import type { GitHubEventFact, GitHubProfileFact } from "@/lib/github/types";

type RecordedDeveloper = { capturedAt: string; profile: GitHubProfileFact; events: GitHubEventFact[] };
const push = (id: string, developer: number, login: string, repoId: number, repo: string, occurredAt: string, head: string): GitHubEventFact => ({ id, type: "PushEvent", actor: { id: developer, login }, repo: { id: repoId, name: repo }, created_at: occurredAt, public: true, payload: { head, ref: "refs/heads/main" } });
const pr = (id: string, developer: number, login: string, repoId: number, repo: string, occurredAt: string, action: string, number: number): GitHubEventFact => ({ id, type: "PullRequestEvent", actor: { id: developer, login }, repo: { id: repoId, name: repo }, created_at: occurredAt, public: true, payload: { action, number, pull_request: {} } });

// Captured from public GitHub event endpoints on 2026-09-17. This is not synthetic activity.
export const recordedDevelopers: Record<string, RecordedDeveloper> = {
  jayjz: { capturedAt: "2026-09-17T21:52:07.000Z", profile: { id: 161224442, login: "jayjz", name: "Jayjz", avatar_url: "https://avatars.githubusercontent.com/u/161224442?v=4", html_url: "https://github.com/jayjz" }, events: [
    push("21413175036", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T16:59:15.000Z", "0747730d18cdb692142d9c2b50f9e8bc1ff0c45f"),
    push("21359142884", 161224442, "jayjz", 1152522353, "jayjz/jayjz", "2026-09-17T00:42:46.000Z", "3cf0d26720a3a9715a693404378a3403379f2881"),
    pr("15188066119", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T02:02:16.000Z", "merged", 2),
    pr("15188054065", 161224442, "jayjz", 1373439999, "jayjz/TEMPER", "2026-09-17T02:01:57.000Z", "opened", 2),
  ] },
  antfu: { capturedAt: "2026-09-16T06:00:00.000Z", profile: { id: 11247099, login: "antfu", name: "Anthony Fu", avatar_url: "https://avatars.githubusercontent.com/u/11247099?v=4", html_url: "https://github.com/antfu" }, events: [
    push("21435990201", 11247099, "antfu", 332422464, "antfu/antfu.me", "2026-09-16T05:02:19.000Z", "e3ff0442327a24b1f389982a19ec8aca704d9245"),
    push("21432446142", 11247099, "antfu", 310226997, "antfu-collective/ni", "2026-09-16T03:24:12.000Z", "2d8c88e9f0d302da75eda36e1a158141714a351e"),
  ] },
  shuding: { capturedAt: "2026-09-16T08:00:00.000Z", profile: { id: 3676859, login: "shuding", name: "Shu Ding", avatar_url: "https://avatars.githubusercontent.com/u/3676859?v=4", html_url: "https://github.com/shuding" }, events: [
    push("21405999173", 3676859, "shuding", 1362860356, "vercel-labs/gpu-lexer", "2026-09-16T03:43:59.000Z", "31b4b5da534947ffb0fb95b9e9337e6ff986b0fc"),
    push("21286439582", 3676859, "shuding", 1362860356, "vercel-labs/gpu-lexer", "2026-09-16T07:32:49.000Z", "18299a78b244e3c6508bbc510ffea9f8b5585d18"),
  ] },
};
</file>

<file path="src/lib/developers.test.ts">
import { describe, expect, it, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { loadDeveloper } from "./developers";

describe("loadDeveloper live-source failures", () => {
  it("does not present a recorded profile for a definitive 404", async () => {
    vi.stubEnv("BUILDBLOCK_DATA_MODE", "live");
    vi.stubGlobal("fetch", vi.fn(async () => new Response(null, { status: 404 })));
    await expect(loadDeveloper("jayjz")).resolves.toBeNull();
    vi.unstubAllGlobals();
    vi.unstubAllEnvs();
  });

  it("uses the explicit recorded source for transient upstream failure", async () => {
    vi.stubEnv("BUILDBLOCK_DATA_MODE", "live");
    vi.stubGlobal("fetch", vi.fn(async () => new Response(null, { status: 503 })));
    await expect(loadDeveloper("jayjz")).resolves.toMatchObject({ source: "recorded" });
    vi.unstubAllGlobals();
    vi.unstubAllEnvs();
  });
});
</file>

<file path=".env.example">
GITHUB_TOKEN=
BUILDBLOCK_DATA_MODE=live
</file>

<file path="AGENTS.md">
# BuildBlock

BuildBlock is a persistent multiplayer developer world where real software activity changes the world.

## Product invariants

- Real software work drives world state.
- Every developer should have a recognizable place in the world.
- GitHub source facts must remain separate from game interpretation.
- World state should be deterministic and explainable.
- Collaboration should eventually matter more than raw activity volume.
- The initial product must work without AI.
- AI agents are a future inhabitant of the world, not the foundation of the product.

## Non-goals

Do not turn BuildBlock into:

- a GitHub analytics dashboard with game decoration
- a generic social network
- a commit-count leaderboard
- a Monopoly clone
- a crypto/token project
- an AI agent marketplace
- a premature microservice system

## Architecture boundary

Preserve this dependency direction:

GitHub source data
→ normalized domain facts
→ deterministic world rules
→ derived world state
→ presentation

React components must not interpret raw GitHub API payloads directly.

Keep network IO outside core world logic.

Prefer pure functions for world-state derivation.

## Engineering rules

- TypeScript strictness.
- No fabricated GitHub activity.
- Fixture data must be identifiable as fixtures.
- Live GitHub failure must degrade gracefully.
- No opaque scoring.
- No premature persistence or authentication for P0.
- No unused speculative abstractions.
- Core world-rule calculations require unit tests.
- Accessibility is required.
- Never commit credentials.
- Verify before claiming completion.

## Current phase

P0: prove that a living GitHub-driven developer board is visually and conceptually compelling.

Do not allow later multiplayer, persistence, authentication, or agent phases to contaminate P0.
</file>

<file path="CLAUDE.md">
@AGENTS.md
</file>

<file path="eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
</file>

<file path="postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path="vitest.config.ts">
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    environment: "node",
  },
});
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = { title: "BuildBlock — District 01", description: "A living developer district shaped by public GitHub activity." };
export default function RootLayout({ children }: LayoutProps<"/">) { return <html lang="en"><body>{children}</body></html>; }
</file>

<file path="src/app/page.tsx">
import { DistrictBoard } from "@/components/district-board";
import { loadDeveloper } from "@/lib/developers";

export default async function Home() {
  const loaded = await Promise.all(["jayjz", "antfu", "shuding"].map(loadDeveloper));
  return <main><DistrictBoard developers={loaded.filter((entry): entry is NonNullable<typeof entry> => entry !== null)} /></main>;
}
</file>

<file path="src/components/district-board.tsx">
"use client";

import { useEffect, useRef, useState } from "react";
import { boardSpaces } from "@/data/board";
import { curatedDevelopers } from "@/data/developers";
import type { DeveloperExperience } from "@/lib/developers";
import { DeveloperWorkshop } from "./developer-workshop";
import { DistrictCommons } from "./district-commons";
import { WorkshopDetail } from "./workshop-detail";

const position = (index: number) => {
  if (index <= 10) return `1 / ${index + 1}`;
  if (index <= 20) return `${index - 9} / 11`;
  if (index <= 30) return `11 / ${31 - index}`;
  return `${41 - index} / 1`;
};

const address = (value: number) => value.toString().padStart(2, "0");

export function DistrictBoard({ developers }: { developers: DeveloperExperience[] }) {
  const [selected, setSelected] = useState<DeveloperExperience | null>(null);
  const originRef = useRef<HTMLButtonElement | null>(null);
  const workshopRefs = useRef(new Map<string, HTMLButtonElement>());
  const byLogin = new Map(developers.map((experience) => [experience.developer.login, experience]));
  const available = [...developers].sort((a, b) => a.developer.address - b.developer.address);

  useEffect(() => {
    if (!selected) return;
    workshopRefs.current.get(selected.developer.login)?.scrollIntoView({ behavior: "auto", block: "nearest", inline: "nearest" });
  }, [selected]);

  const inspect = (experience: DeveloperExperience, origin?: HTMLButtonElement | null) => {
    if (origin) originRef.current = origin;
    setSelected(experience);
  };
  const close = () => {
    setSelected(null);
    requestAnimationFrame(() => originRef.current?.focus());
  };
  const currentIndex = selected ? available.findIndex((experience) => experience.developer.login === selected.developer.login) : -1;
  const nextExperience = currentIndex >= 0 && available.length > 1 ? available[(currentIndex + 1) % available.length]! : undefined;
  const next = () => {
    if (!nextExperience) return;
    originRef.current = workshopRefs.current.get(nextExperience.developer.login) ?? null;
    setSelected(nextExperience);
  };

  return <div className="district-layout">
    <section className="board-shell" aria-label="BuildBlock District 01 board">
      <div className="district-board">
        <div className="district-map" aria-label="District 01 orientation map">
          {boardSpaces.map((space) => {
            const experience = space.login ? byLogin.get(space.login) : undefined;
            if (space.kind === "developer") {
              const developer = curatedDevelopers.find((entry) => entry.login === space.login)!;
              return <div key={space.index} className={`space space--developer${selected?.developer.login === developer.login ? " space--selected" : ""}`} style={{ gridArea: position(space.index) }}>
                {experience ? <button type="button" className="address-anchor" onClick={(event) => inspect(experience, event.currentTarget)} aria-label={`Inspect ${experience.profile.name ?? experience.profile.login}, address ${address(developer.address)}`}>{address(developer.address)}</button> : <span className="address-anchor">{address(developer.address)}</span>}
              </div>;
            }
            if (space.kind === "civic") return <div key={space.index} className="space space--civic" style={{ gridArea: position(space.index) }}><span>{space.label}</span></div>;
            return <div key={space.index} className="space space--open" style={{ gridArea: position(space.index) }} aria-label={`Surveyed open plot ${address(space.index)}`}><span>{address(space.index)}</span></div>;
          })}
          <DistrictCommons />
        </div>
        <div className="district-parcels" aria-label="Developer workshops">
          {available.map((experience) => <DeveloperWorkshop key={experience.developer.login} developer={experience.developer} experience={experience} selected={selected?.developer.login === experience.developer.login} onInspect={() => inspect(experience, workshopRefs.current.get(experience.developer.login))} workshopRef={(element) => { if (element) workshopRefs.current.set(experience.developer.login, element); else workshopRefs.current.delete(experience.developer.login); }} />)}
        </div>
      </div>
    </section>
    {selected && <WorkshopDetail experience={selected} onClose={close} onNext={next} nextName={nextExperience?.profile.name ?? nextExperience?.profile.login} hasNext={Boolean(nextExperience)} />}
  </div>;
}
</file>

<file path="src/data/developers.ts">
export type CuratedDeveloper = { id: number; login: string; address: number; color: string; shape: "tower" | "row" | "workshop" };
export const curatedDevelopers: CuratedDeveloper[] = [
  { id: 161224442, login: "jayjz", address: 1, color: "ochre", shape: "workshop" },
  { id: 11247099, login: "antfu", address: 5, color: "terracotta", shape: "tower" },
  { id: 3676859, login: "shuding", address: 11, color: "slate", shape: "row" },
];
export const findDeveloper = (login: string) => curatedDevelopers.find((developer) => developer.login === login);
</file>

<file path="src/lib/github/normalize.test.ts">
import { describe, expect, it } from "vitest";
import { normalizeActivities } from "./normalize";
import type { GitHubEventFact } from "./types";

const event = (overrides: Partial<GitHubEventFact> = {}): GitHubEventFact => ({ id: "1", type: "PushEvent", actor: { id: 7, login: "dev" }, repo: { id: 9, name: "dev/repo" }, created_at: "2026-09-17T12:00:00.000Z", public: true, payload: { head: "abc" }, ...overrides });
describe("normalizeActivities", () => { it("deduplicates pushes and attributes actor IDs", () => { const push = event(); expect(normalizeActivities([push, { ...push, id: "2" }, event({ actor: { id: 8, login: "other" } })], 7)).toHaveLength(1); }); it("accepts merged but not merely closed pull requests", () => { const closed = event({ type: "PullRequestEvent", payload: { action: "closed", number: 4, pull_request: { merged: false } } }); const merged = event({ id: "3", type: "PullRequestEvent", payload: { action: "merged", number: 4, pull_request: {} } }); expect(normalizeActivities([closed, merged], 7).map((item) => item.kind)).toEqual(["pull_request_merged"]); }); it("excludes unsupported events", () => expect(normalizeActivities([event({ type: "WatchEvent", payload: {} })], 7)).toEqual([])); });

describe("important normalized activity facts", () => {
  it("maps releases and merges to durable evidence URLs", () => {
    const release = event({ id: "release-event", type: "ReleaseEvent", payload: { action: "published", release: { id: 12, html_url: "https://github.com/dev/repo/releases/tag/v1" } } });
    const merge = event({ id: "merge-event", type: "PullRequestEvent", payload: { action: "merged", number: 4, pull_request: { html_url: "https://github.com/dev/repo/pull/4" } } });
    expect(normalizeActivities([release, merge], 7)).toEqual(expect.arrayContaining([
      expect.objectContaining({ kind: "release_published", id: "release:9:12", evidenceUrl: "https://github.com/dev/repo/releases/tag/v1" }),
      expect.objectContaining({ kind: "pull_request_merged", id: "pr-merged:9:4", evidenceUrl: "https://github.com/dev/repo/pull/4" }),
    ]));
  });

  it("selects duplicate representations independently of input order", () => {
    const earlier = event({ id: "z-event", created_at: "2026-09-17T12:00:00.000Z" });
    const later = event({ id: "a-event", created_at: "2026-09-17T13:00:00.000Z" });
    expect(normalizeActivities([earlier, later], 7)).toEqual(normalizeActivities([later, earlier], 7));
    expect(normalizeActivities([earlier, later], 7)[0]?.occurredAt).toBe("2026-09-17T12:00:00.000Z");
  });

  it("excludes private and wrong-actor events", () => {
    expect(normalizeActivities([event({ public: false }), event({ actor: { id: 8, login: "other" } })], 7)).toEqual([]);
  });
});
</file>

<file path="src/lib/github/types.ts">
import { z } from "zod";

const actorSchema = z.object({ id: z.number(), login: z.string() });
const repositorySchema = z.object({ id: z.number(), name: z.string() });

export const githubProfileSchema = z.object({
  id: z.number(), login: z.string(), name: z.string().nullable(), avatar_url: z.string().url(), html_url: z.string().url(),
});

export const githubEventSchema = z.object({
  id: z.string(), type: z.string(), actor: actorSchema, repo: repositorySchema, created_at: z.string().datetime(), public: z.boolean().optional().default(true),
  payload: z.object({
    head: z.string().optional(), ref: z.string().optional(), action: z.string().optional(), number: z.number().optional(),
    pull_request: z.object({ merged: z.boolean().nullable().optional(), html_url: z.string().url().nullable().optional() }).optional(),
    release: z.object({ id: z.number().optional(), html_url: z.string().url().nullable().optional() }).optional(),
  }).passthrough(),
}).passthrough();

export type GitHubProfileFact = z.infer<typeof githubProfileSchema>;
export type GitHubEventFact = z.infer<typeof githubEventSchema>;
export type ActivityKind = "code_pushed" | "pull_request_opened" | "pull_request_merged" | "release_published";

export type NormalizedActivity = {
  id: string; kind: ActivityKind; developerId: number; login: string; repository: { id: number; name: string };
  occurredAt: string; evidenceUrl: string; sourceEventIds: string[];
};
</file>

<file path="src/lib/world/rules.ts">
import type { NormalizedActivity } from "@/lib/github/types";
import type { WorldEvent } from "./types";

export function deriveWorldEvents(activities: NormalizedActivity[]): WorldEvent[] {
  return activities.flatMap((activity) => {
    const events: WorldEvent[] = [{ id: `workday:${activity.id}`, kind: "workday_observed", effectiveAt: activity.occurredAt, activity }];
    if (activity.kind === "pull_request_merged") events.push({ id: `merge:${activity.id}`, kind: "merge_observed", effectiveAt: activity.occurredAt, activity });
    if (activity.kind === "release_published") events.push({ id: `release:${activity.id}`, kind: "release_observed", effectiveAt: activity.occurredAt, activity });
    return events;
  });
}
</file>

<file path="src/lib/developers.ts">
import "server-only";
import { findDeveloper, type CuratedDeveloper } from "@/data/developers";
import { recordedDevelopers } from "@/data/fixtures";
import { normalizeActivities } from "@/lib/github/normalize";
import { githubEventSchema, githubProfileSchema, type GitHubEventFact, type GitHubProfileFact, type NormalizedActivity } from "@/lib/github/types";
import { deriveBlockState } from "@/lib/world/derive";

export type DeveloperExperience = { developer: CuratedDeveloper; profile: GitHubProfileFact; activities: NormalizedActivity[]; state: ReturnType<typeof deriveBlockState>; source: "live" | "recorded"; capturedAt: string };
const githubHeaders = { Accept: "application/vnd.github+json", "X-GitHub-Api-Version": "2026-03-10", ...(process.env.GITHUB_TOKEN ? { Authorization: `Bearer ${process.env.GITHUB_TOKEN}` } : {}) };
class GitHubProfileNotFoundError extends Error {}

async function getLive(login: string) {
  const [profileResponse, eventResponse] = await Promise.all([
    fetch(`https://api.github.com/users/${login}`, { headers: githubHeaders, next: { revalidate: 3600 } }),
    fetch(`https://api.github.com/users/${login}/events/public?per_page=100`, { headers: githubHeaders, next: { revalidate: 3600 } }),
  ]);
  if (profileResponse.status === 404) throw new GitHubProfileNotFoundError("GitHub profile was not found");
  if (!profileResponse.ok || !eventResponse.ok) throw new Error("GitHub public data is unavailable");
  return { profile: githubProfileSchema.parse(await profileResponse.json()), events: githubEventSchema.array().parse(await eventResponse.json()) };
}

export async function loadDeveloper(login: string): Promise<DeveloperExperience | null> {
  const developer = findDeveloper(login); if (!developer) return null;
  const recorded = recordedDevelopers[login];
  const mode = process.env.BUILDBLOCK_DATA_MODE;
  let source: "live" | "recorded" = "recorded";
  let profile = recorded.profile; let events: GitHubEventFact[] = recorded.events; let capturedAt = recorded.capturedAt;
  if (mode !== "recorded") {
    try {
      const live = await getLive(login); profile = live.profile; events = live.events; capturedAt = new Date().toISOString(); source = "live";
    } catch (error) {
      if (error instanceof GitHubProfileNotFoundError) return null;
      /* transient live-source failures leave the explicit recorded source visible */
    }
  }
  const activities = normalizeActivities(events, developer.id);
  return { developer, profile, activities, state: deriveBlockState(activities, capturedAt), source, capturedAt };
}
</file>

<file path="next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [{ protocol: "https", hostname: "avatars.githubusercontent.com" }],
  },
};

export default nextConfig;
</file>

<file path="README.md">
# BuildBlock

BuildBlock is a persistent developer world where public GitHub activity changes a recognizable block in a shared district.

## Run locally

```bash
npm install
npm run dev
```

For a reproducible offline board, copy `.env.example` to `.env.local` and set `BUILDBLOCK_DATA_MODE=recorded`.

## Verify

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

See `docs/` for product, architecture, world-rule, and roadmap decisions.
</file>

<file path="docs/ARCHITECTURE.md">
# Architecture

The dependency direction is fixed: GitHub source responses → validated facts → normalized activity → deterministic world events and block state → presentation.

`src/lib/github` validates and normalizes public REST responses. `src/lib/world` is pure and has no network or clock access. `deriveBlockState` applies the observation window once and derives deterministic repository groups, including repository-specific merge and release booleans, for every eligible normalized activity. `src/lib/developers` is server-only composition; it fetches live public data and falls back to explicitly recorded source facts. React receives `DeveloperExperience`, never raw GitHub responses, and renders the canonical groups without regrouping activity.

P0 uses only three configured public profiles and no persistence, authentication, or backend services. Next fetch revalidation is the only cache behavior.
</file>

<file path="docs/PRODUCT.md">
# BuildBlock P0

BuildBlock is a persistent developer world where real software work changes a recognizable place. P0 tests whether an evidence-backed public GitHub view makes a developer want a block.

District 01 is a crafted miniature district, not a ranking or analytics dashboard. A block is a permanent workshop with a fixed address and architectural identity. Its frontage shows up to three repositories recently observed in the current window; bounded merge and release states appear with the relevant repository work bay. Curated public profiles do not imply participation or endorsement.

The public event feed is incomplete. BuildBlock therefore says “recently observed here” and “No public work observed in this window,” never “all activity,” “productivity,” or “inactive.”
</file>

<file path="docs/ROADMAP.md">
# Roadmap

P0 is a public, evidence-backed district with stable workshops and temporary repository-fronted observed-work state. It deliberately excludes ownership, history, multiplayer, authentication, rankings, and AI.

The next gate is qualitative: visitors should recognize a block, explain its visible state through evidence, and want their own place. Only after that should BuildBlock evaluate claimed identity and durable history; collaboration needs verified attribution before it becomes a world rule. Agents are a later inhabitant, never a P0 foundation.
</file>

<file path="docs/WORLD_RULES.md">
# World rules: p0-v1

The observation window is the `asOf` UTC day plus the six preceding UTC days, ending at `asOf`. Each supported activity on a day emits one `workday_observed` event; repeated work on that day does not create more frontage state.

`code_pushed`, `pull_request_opened`, `pull_request_merged`, and `release_published` create eligible observed work for that activity's repository. Repository groups are ordered by their latest eligible observation and repository ID, with a most-recent name resolved deterministically. A merge creates one repository-specific joined-work state and a release one repository-specific publication state; repeated events do not multiply either state. Activities are deduplicated by their durable action identity and only source events whose actor ID matches the configured developer are accepted.

Stars, forks, branch creation, comments, counts, and follower data have no effect. A closed pull request is a merge only when GitHub explicitly reports `merged` or `closed` with `merged: true`.
</file>

<file path="src/app/globals.css">
@import "tailwindcss";

:root { --ink: #18302b; --paper: #f5f0e5; --grass: #7d986b; --road: #d6c9ae; --line: #33483e; --clay: #a65b29; }
* { box-sizing: border-box; }
html { overflow-x: hidden; overflow-y: scroll; }
body { margin: 0; color: var(--ink); background: #e5dccb; font-family: Georgia, "Times New Roman", serif; }
button, a { font: inherit; }
button { color: inherit; }
a { color: #80421e; text-underline-offset: 2px; }
main { min-height: 100vh; padding: clamp(12px, 3vw, 42px); background: radial-gradient(circle at 50% 22%, #f8f4eb 0 17%, transparent 48%), #dfe5d2; }

.district-layout { display: grid; gap: clamp(22px, 3vw, 38px); max-width: 1280px; margin: auto; }
.board-shell { min-width: 0; border: 1px solid #83917c; background: #becaae; box-shadow: 0 18px 50px #40523935; padding: clamp(8px, 1.3vw, 14px); }
.district-board { width: min(100%, 1240px); height: clamp(640px, 54vw, 680px); position: relative; margin: auto; background: #d8e2c8; }
.district-map { position: absolute; inset: 0; display: grid; grid-template: 52px repeat(9, minmax(0, 1fr)) 52px / repeat(11, minmax(0, 1fr)); overflow: hidden; background: linear-gradient(180deg, #edf0df, #d7e1c8); }
.space { min-width: 0; min-height: 0; display: flex; align-items: center; justify-content: center; position: relative; border: 1px solid #a89675; background: var(--road); color: #465443; font: 10px ui-monospace, monospace; letter-spacing: .08em; text-transform: uppercase; }
.space--developer { background: #e8d9b8; }
.space--developer.space--selected { background: #dfc77f; }
.address-anchor { width: 100%; min-height: 44px; display: grid; place-items: center; border: 0; background: transparent; color: #4b3d2c; cursor: pointer; font: 12px ui-monospace, monospace; }
button.address-anchor:hover, button.address-anchor:focus-visible { outline: 3px solid #d58832; outline-offset: -4px; background: #f7eccf; }
.space--civic { padding: 5px; background: #526a5b; color: #f7efd9; font: 700 9px/1.15 Georgia, serif; letter-spacing: .04em; text-align: center; }
.space--civic span::before { content: "✦ "; color: #e8d391; }
.space--open { color: #718064; background: #d8cbae; }
.space--open span { padding: 3px; border-bottom: 1px solid #9b966f; }

.district-commons { grid-area: 2 / 2 / 11 / 11; z-index: 0; display: grid; align-content: end; justify-items: center; gap: 10px; padding: 24px; position: relative; overflow: hidden; background: linear-gradient(90deg, transparent 0 48%, #c1cfaa 48% 52%, transparent 52%), linear-gradient(#dfe8cc, #c2d0ac); }
.district-commons::before { content: ""; position: absolute; left: 8%; right: 8%; bottom: 16%; height: 16px; border-top: 1px solid #9faf8d; border-bottom: 1px solid #a8b892; background: #c6d3af; }
.commons-copy { position: relative; z-index: 1; padding: 7px 13px; border: 1px solid #849077; background: #ecdfbd; text-align: center; }
.eyebrow { margin: 0; color: var(--clay); font: 10px ui-monospace, monospace; letter-spacing: .12em; }
.commons-copy h1 { margin: 3px 0; font-size: clamp(19px, 2vw, 27px); letter-spacing: -.045em; line-height: 1; }
.commons-copy p:not(.eyebrow) { margin: 0; color: #526051; font-size: 11px; }
.evidence-key { position: relative; z-index: 1; display: flex; flex-wrap: wrap; justify-content: center; gap: 8px 14px; max-width: 620px; color: #566253; font: 10px/1.25 ui-monospace, monospace; }
.evidence-key span { display: inline-flex; align-items: center; gap: 5px; }
.key-object { width: 18px; height: 12px; display: inline-flex; align-items: center; justify-content: center; }
.key-object--merge b { width: 9px; height: 9px; display: block; background: #d4c28d; border: 1px solid #6d6047; }
.key-object--merge b:first-child { clip-path: polygon(0 0, 100% 0, 100% 67%, 70% 67%, 70% 100%, 0 100%); }
.key-object--merge b:last-child { margin-left: -1px; clip-path: polygon(0 0, 30% 0, 30% 33%, 100% 33%, 100% 100%, 0 100%); }
.key-object--release { border: 1px solid #677354; background: #c9d2ae; }
.key-object--release b { width: 8px; height: 3px; border: 1px solid #677354; background: #eee7d0; }

.district-parcels { position: absolute; z-index: 2; inset: 82px 5.8% 52px; display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); align-items: start; gap: clamp(14px, 2vw, 30px); pointer-events: none; }
.workshop { --workshop-wall: #bd7a42; appearance: none; width: 100%; min-width: 0; display: grid; justify-items: stretch; gap: 0; padding: 24px 16px 14px; border: 1px solid #69765e; background: linear-gradient(180deg, #dce6cd 0 38%, #d5c9ad 38% 100%); box-shadow: 4px 5px 0 #56644f55; color: var(--ink); cursor: pointer; pointer-events: auto; text-align: left; position: relative; }
.workshop:focus-visible { outline: 4px solid #d58832; outline-offset: 3px; z-index: 4; }
.workshop[aria-pressed="true"] { border-color: #9a672f; box-shadow: 0 0 0 3px #e1a34c, 4px 5px 0 #56644f55; }
.workshop--terracotta { --workshop-wall: #a75d46; }
.workshop--slate { --workshop-wall: #71838a; }
.workshop--unavailable { cursor: default; filter: grayscale(.72); }
.workshop-building { width: 72%; min-width: 180px; max-width: 252px; justify-self: center; display: grid; filter: drop-shadow(3px 4px 0 #4f5a4665); }
.workshop--tower .workshop-building { width: 53%; max-width: 196px; }
.workshop--row .workshop-building { width: 88%; max-width: 300px; }
.workshop-roof { height: 43px; display: block; background: #4d4b42; clip-path: polygon(50% 0, 100% 100%, 0 100%); position: relative; z-index: 1; }
.workshop--tower .workshop-roof { height: 36px; width: 78%; justify-self: center; }
.workshop--row .workshop-roof { height: 28px; clip-path: polygon(7% 0, 93% 0, 100% 100%, 0 100%); }
.workshop-roof i { position: absolute; bottom: 2px; width: 2px; height: 24px; background: #776e5c; }
.workshop-roof i:first-child { left: 38%; }
.workshop-roof i:last-child { right: 38%; }
.workshop-facade { min-height: 82px; display: grid; grid-template-columns: 1fr 1.05fr 1fr; align-items: end; gap: 12px; padding: 13px 16px 0; border: 3px solid #554633; background: var(--workshop-wall); }
.workshop--tower .workshop-facade { min-height: 116px; grid-template-columns: 1fr; padding-inline: 20px; }
.workshop--tower .workshop-window { display: none; }
.workshop--row .workshop-facade { min-height: 68px; }
.workshop-window { height: 30px; border: 3px solid #554633; border-bottom: 0; background: linear-gradient(90deg, #3e5149 47%, #8ca08a 48% 52%, #3e5149 53%); }
.workshop-door { height: 51px; border: 3px solid #554633; border-bottom: 0; background: #554633; }
.workshop-nameplate { justify-self: center; max-width: 94%; margin-top: -2px; padding: 5px 9px; border: 1px solid #28342d; background: #213d34; color: #f6edd8; font-size: 16px; line-height: 1.1; overflow-wrap: anywhere; text-align: center; }
.workshop-nameplate small { display: block; margin-top: 2px; color: #d8e5c5; font: 10px ui-monospace, monospace; }
.workshop-threshold { width: max-content; max-width: 88%; min-height: 32px; justify-self: center; display: inline-flex; align-items: center; gap: 7px; padding: 4px 8px; border: 1px solid #514331; background: #efe1bc; color: #463827; font: 11px ui-monospace, monospace; }
.workshop-threshold strong { font-size: 15px; }
.workshop-threshold em { color: #6e624d; font-size: 9px; font-style: normal; letter-spacing: .08em; text-transform: uppercase; }
.work-apron { width: 100%; margin-top: 13px; display: grid; border: 1px solid #80755d; background: #e4d8bb; box-shadow: inset 0 0 0 4px #d3c5a7; }
.apron-heading { display: block; padding: 7px 10px 5px; color: #57654d; font: 10px ui-monospace, monospace; letter-spacing: .08em; text-transform: uppercase; }
.workbench { display: grid; padding: 0 7px 7px; background: linear-gradient(#9c724b 0 6px, transparent 6px); }
.repository-placard { min-width: 0; min-height: 42px; display: flex; align-items: center; gap: 10px; padding: 8px 9px; border: 1px solid #5a513f; border-top: 0; background: #f5eedb; box-shadow: inset 0 -4px 0 #cdbf9d; }
.repository-name { min-width: 0; flex: 1; color: #243b32; font: 600 16px/1.2 ui-monospace, monospace; overflow-wrap: anywhere; word-break: normal; }
.repository-objects { display: inline-flex; flex: 0 0 auto; align-items: center; gap: 6px; }
.repository-object { display: inline-flex; align-items: center; justify-content: center; width: 29px; height: 18px; }
.repository-object--merge i { width: 14px; height: 14px; display: block; background: #d4c28d; border: 1px solid #6d6047; }
.repository-object--merge i:first-child { clip-path: polygon(0 0, 100% 0, 100% 64%, 66% 64%, 66% 100%, 0 100%); }
.repository-object--merge i:last-child { margin-left: -1px; clip-path: polygon(0 0, 34% 0, 34% 36%, 100% 36%, 100% 100%, 0 100%); }
.repository-object--release { width: 28px; height: 19px; border: 1px solid #677354; background: #c9d2ae; box-shadow: inset 0 -3px 0 #aab591; }
.repository-object--release i { width: 12px; height: 5px; display: block; border: 1px solid #677354; background: #eee7d0; }
.workbench-empty { min-height: 42px; display: flex; align-items: center; padding: 9px; border: 1px solid #5a513f; border-top: 0; background: #f5eedb; color: #586152; font: 14px/1.25 Georgia, serif; }
.workshop-more { display: block; padding: 5px 10px 2px; color: #57654d; font: 11px/1.25 ui-monospace, monospace; }
.workshop-source { display: block; padding: 7px 10px; border-top: 1px solid #b6a984; color: #596355; font: 10px/1.25 ui-monospace, monospace; }

.workshop-detail { width: min(100%, 1240px); margin: 0 auto; background: #f9f4e8; border: 1px solid #9c8d72; box-shadow: 0 16px 55px #263c3350; padding: clamp(18px, 2.3vw, 28px); }
.detail-topline { display: flex; justify-content: space-between; align-items: center; gap: 12px; }
.detail-close { min-height: 44px; padding: 7px 10px; border: 1px solid #8b785a; background: #e8dcc0; cursor: pointer; }
.detail-close:hover, .detail-close:focus-visible, .next-workshop:hover, .next-workshop:focus-visible { outline: 3px solid #d58832; outline-offset: 2px; }
.detail-heading { display: flex; gap: 14px; align-items: center; margin-top: 10px; }
.detail-heading img { border-radius: 50%; }
.detail-heading h2 { margin: 0 0 3px; }
.detail-heading h2:focus { outline: 3px solid #d58832; outline-offset: 3px; }
.detail-facts { margin: 20px 0; padding: 10px 0; border-top: 1px solid #d9cdb5; border-bottom: 1px solid #d9cdb5; }
.detail-facts div { margin: 8px 0; }
.detail-facts dt { color: #6a725e; font: 10px ui-monospace, monospace; letter-spacing: .08em; text-transform: uppercase; }
.detail-facts dd { margin: 2px 0 0; font-size: 13px; line-height: 1.35; }
.workshop-detail section { margin-top: 20px; }
.workshop-detail h3 { margin: 0 0 8px; }
.workshop-detail p, .workshop-detail li { font-size: 14px; line-height: 1.45; }
.repository-list { list-style: none; padding: 0; margin: 0; }
.repository-group { border-top: 1px solid #d9cdb5; padding: 12px 0; }
.repository-heading { display: flex; flex-direction: column; gap: 3px; overflow-wrap: anywhere; }
.repository-heading > a { font-weight: bold; }
.repository-heading span, .evidence-list time { color: #667069; font-size: 11px; }
.repository-state { display: flex; flex-wrap: wrap; gap: 5px; margin: 8px 0; }
.repository-state span { padding: 2px 5px; border: 1px solid #9c8d72; font: 10px ui-monospace, monospace; }
.observed-work { background: #e4ecd7; }
.merge-state { background: #eee0b1; clip-path: polygon(4px 0, calc(100% - 4px) 0, 100% 50%, calc(100% - 4px) 100%, 4px 100%, 0 50%); }
.release-state { background: #d5dfc3; border-bottom: 3px double #526a4c !important; }
.evidence-list { padding-left: 18px; }
.evidence-list li { margin: 6px 0; }
.evidence-list time { display: block; }
.detail-limits { border-top: 1px solid #d9cdb5; padding-top: 12px; color: #596355; }
.next-workshop { width: 100%; min-height: 48px; margin-top: 20px; border: 1px solid #31463d; background: #213d34; color: white; cursor: pointer; }
.next-workshop span { margin-left: 4px; }

@media (max-width: 1179px) {
  .district-layout { max-width: 720px; }
  .board-shell { padding: 0; border: 0; background: transparent; box-shadow: none; }
  .district-board { width: 100%; height: auto; display: grid; gap: 24px; background: transparent; }
  .district-map { position: relative; width: 100%; aspect-ratio: 1; display: grid; grid-template: repeat(11, minmax(0, 1fr)) / repeat(11, minmax(0, 1fr)); border: 1px solid #83917c; box-shadow: 0 9px 24px #40523925; }
  .space { font-size: clamp(6px, 1.25vw, 10px); }
  .space--civic { font-size: clamp(5px, 1vw, 8px); }
  .address-anchor { min-height: 44px; font-size: 12px; }
  .district-commons { grid-area: 4 / 4 / 9 / 9; align-content: center; gap: 7px; padding: 8px; }
  .district-commons::before { left: 6%; right: 6%; bottom: 14%; height: 9px; }
  .commons-copy { padding: 5px 8px; }
  .commons-copy h1 { font-size: clamp(16px, 4.4vw, 25px); }
  .commons-copy p:not(.eyebrow), .evidence-key { display: none; }
  .district-parcels { position: static; display: grid; grid-template-columns: minmax(0, 620px); justify-content: center; gap: 24px; pointer-events: auto; }
  .workshop { min-height: 0; padding: 24px clamp(12px, 4vw, 28px) 16px; }
  .workshop-building { min-width: min(180px, 65vw); }
  .workshop--tower .workshop-building { min-width: 140px; }
  .workshop-detail { margin: 0; }
}

@media (max-width: 700px) {
  main { padding: 0; }
  .district-layout { gap: 18px; }
  .district-map { border-left: 0; border-right: 0; }
  .space--open span { padding: 1px; }
  .space--civic { padding: 2px; }
  .workshop-detail { border-left: 0; border-right: 0; }
  .workshop-nameplate { font-size: 16px; }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { scroll-behavior: auto !important; transition: none !important; animation: none !important; }
}
</file>

<file path="src/lib/github/normalize.ts">
import type { GitHubEventFact, NormalizedActivity } from "./types";

const repositoryUrl = (name: string) => `https://github.com/${name}`;

const duplicatePreferenceKey = (activity: NormalizedActivity) => [
  activity.occurredAt,
  activity.evidenceUrl,
  activity.login,
  activity.repository.name,
  activity.sourceEventIds[0] ?? "",
].join("\u0000");

function preferDuplicate(current: NormalizedActivity, candidate: NormalizedActivity) {
  return duplicatePreferenceKey(candidate) < duplicatePreferenceKey(current) ? candidate : current;
}

export function normalizeActivities(events: GitHubEventFact[], developerId: number): NormalizedActivity[] {
  const accepted = events
    .filter((event) => event.public && event.actor.id === developerId)
    .flatMap((event): NormalizedActivity[] => {
      const base = { developerId, login: event.actor.login, repository: event.repo, occurredAt: event.created_at, sourceEventIds: [event.id] };
      if (event.type === "PushEvent" && event.payload.head) return [{ ...base, id: `push:${event.repo.id}:${event.payload.head}`, kind: "code_pushed", evidenceUrl: `${repositoryUrl(event.repo.name)}/commit/${event.payload.head}` }];
      if (event.type === "PullRequestEvent" && event.payload.number) {
        const url = event.payload.pull_request?.html_url ?? `${repositoryUrl(event.repo.name)}/pull/${event.payload.number}`;
        if (event.payload.action === "opened") return [{ ...base, id: `pr-opened:${event.repo.id}:${event.payload.number}`, kind: "pull_request_opened", evidenceUrl: url }];
        if (event.payload.action === "merged" || (event.payload.action === "closed" && event.payload.pull_request?.merged === true)) return [{ ...base, id: `pr-merged:${event.repo.id}:${event.payload.number}`, kind: "pull_request_merged", evidenceUrl: url }];
      }
      if (event.type === "ReleaseEvent" && event.payload.action === "published" && event.payload.release?.id) return [{ ...base, id: `release:${event.repo.id}:${event.payload.release.id}`, kind: "release_published", evidenceUrl: event.payload.release.html_url ?? repositoryUrl(event.repo.name) }];
      return [];
    });
  const deduplicated = new Map<string, NormalizedActivity>();
  for (const activity of accepted) {
    const current = deduplicated.get(activity.id);
    deduplicated.set(activity.id, current ? preferDuplicate(current, activity) : activity);
  }
  return [...deduplicated.values()].sort((a, b) => b.occurredAt.localeCompare(a.occurredAt) || a.id.localeCompare(b.id));
}
</file>

<file path="src/lib/world/derive.test.ts">
import { describe, expect, it } from "vitest";
import { deriveBlockState } from "./derive";
import type { NormalizedActivity } from "@/lib/github/types";
import { normalizeActivities } from "@/lib/github/normalize";
import { recordedDevelopers } from "@/data/fixtures";
const asOf = "2026-09-17T23:30:00.000Z";
const activity = (occurredAt: string, id = occurredAt, repository = { id: 2, name: "dev/repo" }, kind: NormalizedActivity["kind"] = "code_pushed"): NormalizedActivity => ({ id, kind, developerId: 1, login: "dev", repository, occurredAt, evidenceUrl: `https://github.com/${repository.name}`, sourceEventIds: [id] });
describe("deriveBlockState", () => { it("uses exact UTC observation boundaries and stable identity-free state", () => { const state = deriveBlockState([activity("2026-09-11T00:00:00.000Z"), activity("2026-09-17T23:00:00.000Z"), activity("2026-09-10T23:59:59.000Z")], "2026-09-17T23:30:00.000Z"); expect(state.windows).toEqual([true, false, false, false, false, false, true]); }); });

describe("deriveBlockState signals", () => {
  it("derives merge pennants and release beacons from normalized activity", () => {
    const merged = { ...activity("2026-09-17T10:00:00.000Z", "merge"), kind: "pull_request_merged" as const };
    const released = { ...activity("2026-09-16T10:00:00.000Z", "release"), kind: "release_published" as const };
    const state = deriveBlockState([merged, released], "2026-09-17T23:30:00.000Z");
    expect(state.mergeEvents).toHaveLength(1);
    expect(state.releaseEvents).toHaveLength(1);
  });

  it("excludes future activity from the observation window", () => {
    const state = deriveBlockState([activity("2026-09-18T00:00:00.000Z")], "2026-09-17T23:30:00.000Z");
    expect(state.windows).toEqual([false, false, false, false, false, false, false]);
  });
});

describe("observed repository groups", () => {
  it("returns zero groups when there is no eligible observed work", () => {
    expect(deriveBlockState([activity("2026-09-10T23:59:59.000Z"), activity("2026-09-18T00:00:00.000Z")], asOf).repositories).toEqual([]);
  });

  it("groups one repository and retains its eligible activities", () => {
    const state = deriveBlockState([activity("2026-09-17T12:00:00.000Z", "a"), activity("2026-09-16T12:00:00.000Z", "b")], asOf);
    expect(state.repositories).toHaveLength(1);
    expect(state.repositories[0]).toMatchObject({ repository: { id: 2, name: "dev/repo" }, latestObservedAt: "2026-09-17T12:00:00.000Z" });
    expect(state.repositories[0]?.activities.map((item) => item.id)).toEqual(["a", "b"]);
  });

  it("keeps three and more than three repositories in domain state", () => {
    const repositories = [1, 2, 3, 4].map((id) => activity(`2026-09-17T0${id}:00:00.000Z`, `repo-${id}`, { id, name: `dev/repo-${id}` }));
    expect(deriveBlockState(repositories.slice(0, 3), asOf).repositories).toHaveLength(3);
    expect(deriveBlockState(repositories, asOf).repositories).toHaveLength(4);
  });

  it("orders groups by their newest eligible observation then repository ID", () => {
    const state = deriveBlockState([
      activity("2026-09-16T12:00:00.000Z", "older", { id: 9, name: "dev/older" }),
      activity("2026-09-17T12:00:00.000Z", "high", { id: 7, name: "dev/high" }),
      activity("2026-09-17T12:00:00.000Z", "low", { id: 3, name: "dev/low" }),
    ], asOf);
    expect(state.repositories.map((group) => group.repository.id)).toEqual([3, 7, 9]);
  });

  it("is identical when eligible input is reordered", () => {
    const input = [
      activity("2026-09-17T11:00:00.000Z", "b", { id: 5, name: "dev/five" }),
      activity("2026-09-17T12:00:00.000Z", "a", { id: 3, name: "dev/three" }, "pull_request_merged"),
      activity("2026-09-16T12:00:00.000Z", "c", { id: 5, name: "dev/five" }, "release_published"),
    ];
    expect(deriveBlockState(input, asOf).repositories).toEqual(deriveBlockState([...input].reverse(), asOf).repositories);
  });

  it("attributes merge and release state to the individual repository and allows both", () => {
    const state = deriveBlockState([
      activity("2026-09-17T12:00:00.000Z", "merge", { id: 1, name: "dev/one" }, "pull_request_merged"),
      activity("2026-09-17T11:00:00.000Z", "release", { id: 1, name: "dev/one" }, "release_published"),
      activity("2026-09-17T10:00:00.000Z", "other", { id: 2, name: "dev/two" }, "release_published"),
    ], asOf);
    expect(state.repositories[0]).toMatchObject({ repository: { id: 1 }, hasMerge: true, hasRelease: true });
    expect(state.repositories[1]).toMatchObject({ repository: { id: 2 }, hasMerge: false, hasRelease: true });
  });

  it("keeps opened pull requests as observed work without merge state", () => {
    const state = deriveBlockState([activity("2026-09-17T12:00:00.000Z", "opened", { id: 1, name: "dev/one" }, "pull_request_opened")], asOf);
    expect(state.repositories[0]).toMatchObject({ hasMerge: false, hasRelease: false });
    expect(state.repositories[0]?.activities).toHaveLength(1);
  });

  it("uses boolean merge and release semantics for repeated same-kind events", () => {
    const state = deriveBlockState([
      activity("2026-09-17T12:00:00.000Z", "merge-1", { id: 1, name: "dev/one" }, "pull_request_merged"),
      activity("2026-09-17T11:00:00.000Z", "merge-2", { id: 1, name: "dev/one" }, "pull_request_merged"),
      activity("2026-09-17T10:00:00.000Z", "release-1", { id: 1, name: "dev/one" }, "release_published"),
      activity("2026-09-17T09:00:00.000Z", "release-2", { id: 1, name: "dev/one" }, "release_published"),
    ], asOf);
    expect(state.repositories[0]).toMatchObject({ hasMerge: true, hasRelease: true });
  });

  it("resolves a repository name from the newest activity and activity ID on timestamp ties", () => {
    const state = deriveBlockState([
      activity("2026-09-17T12:00:00.000Z", "z", { id: 1, name: "dev/older-name" }),
      activity("2026-09-17T12:00:00.000Z", "a", { id: 1, name: "dev/resolved-name" }),
      activity("2026-09-16T12:00:00.000Z", "newer-by-date", { id: 1, name: "dev/not-current" }),
    ], asOf);
    expect(state.repositories[0]?.repository.name).toBe("dev/resolved-name");
  });

  it("preserves the recorded-mode repository frontage without fabricating release state", () => {
    const stateFor = (login: keyof typeof recordedDevelopers) => {
      const recorded = recordedDevelopers[login];
      return deriveBlockState(normalizeActivities(recorded.events, recorded.profile.id), recorded.capturedAt);
    };
    const jayjz = stateFor("jayjz");
    expect(jayjz.repositories.map((group) => group.repository.name)).toEqual(["jayjz/TEMPER", "jayjz/jayjz"]);
    expect(jayjz.repositories.find((group) => group.repository.name === "jayjz/TEMPER")).toMatchObject({ hasMerge: true, hasRelease: false });
    expect(stateFor("antfu").repositories).toHaveLength(2);
    expect(stateFor("shuding").repositories).toHaveLength(1);
    expect([...jayjz.repositories, ...stateFor("antfu").repositories, ...stateFor("shuding").repositories].some((group) => group.hasRelease)).toBe(false);
  });
});
</file>

<file path="src/lib/world/derive.ts">
import type { NormalizedActivity } from "@/lib/github/types";
import { deriveWorldEvents } from "./rules";
import type { BlockState, ObservedRepository } from "./types";

const dayKey = (value: string) => value.slice(0, 10);

function compareActivities(a: NormalizedActivity, b: NormalizedActivity) {
  return b.occurredAt.localeCompare(a.occurredAt) || a.id.localeCompare(b.id);
}

function deriveRepositories(activities: NormalizedActivity[]): ObservedRepository[] {
  const byRepository = new Map<number, NormalizedActivity[]>();
  for (const activity of activities) {
    const grouped = byRepository.get(activity.repository.id) ?? [];
    grouped.push(activity);
    byRepository.set(activity.repository.id, grouped);
  }

  return [...byRepository.entries()]
    .map(([id, grouped]) => {
      const sorted = [...grouped].sort(compareActivities);
      const latest = sorted[0]!;
      return {
        repository: { id, name: latest.repository.name },
        activities: sorted,
        latestObservedAt: latest.occurredAt,
        hasMerge: sorted.some((activity) => activity.kind === "pull_request_merged"),
        hasRelease: sorted.some((activity) => activity.kind === "release_published"),
      };
    })
    .sort((a, b) => b.latestObservedAt.localeCompare(a.latestObservedAt) || a.repository.id - b.repository.id);
}

export function deriveBlockState(activities: NormalizedActivity[], asOf: string): BlockState {
  const end = new Date(asOf); end.setUTCHours(0, 0, 0, 0);
  const startsAt = new Date(end.getTime() - 6 * 86_400_000).toISOString();
  const days = Array.from({ length: 7 }, (_, index) => new Date(end.getTime() - (6 - index) * 86_400_000).toISOString().slice(0, 10));
  const eligible = activities.filter((activity) => activity.occurredAt <= asOf && days.includes(dayKey(activity.occurredAt)));
  const events = deriveWorldEvents(eligible);
  const merges = events.filter((event) => event.kind === "merge_observed");
  const releases = events.filter((event) => event.kind === "release_observed");
  return { ruleVersion: "p0-v1", asOf, observationWindow: { startsAt, endsAt: asOf }, windows: days.map((day) => eligible.some((activity) => dayKey(activity.occurredAt) === day)), repositories: deriveRepositories(eligible), mergeEvents: merges, releaseEvents: releases,
    explanations: [
      ...merges.map((event) => `Merge pennant: observed merge in ${event.activity.repository.name}.`),
      ...releases.map((event) => `Release beacon: observed release in ${event.activity.repository.name}.`),
    ] };
}
</file>

<file path="src/lib/world/types.ts">
import type { NormalizedActivity } from "@/lib/github/types";

export type WorldEvent = { id: string; kind: "workday_observed" | "merge_observed" | "release_observed"; effectiveAt: string; activity: NormalizedActivity };
export type ObservedRepository = {
  repository: { id: number; name: string };
  activities: NormalizedActivity[];
  latestObservedAt: string;
  hasMerge: boolean;
  hasRelease: boolean;
};

export type BlockState = {
  ruleVersion: "p0-v1";
  asOf: string;
  observationWindow: { startsAt: string; endsAt: string };
  windows: boolean[];
  repositories: ObservedRepository[];
  mergeEvents: WorldEvent[];
  releaseEvents: WorldEvent[];
  explanations: string[];
};
</file>

<file path=".gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local Python environment
/venv/
</file>

<file path="package.json">
{
  "name": "buildblock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.5",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
</file>

</files>
